<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPI_UI.Controls.MainPage.FlashlightTipsWidgetUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Controls.MainPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer">

    <UserControl.Resources>
        <Storyboard x:Name="SlideOutStoryboard">
            <DoubleAnimation x:Name="SlideOutAnimation"
                             Storyboard.TargetName="TextTransform"
                             Storyboard.TargetProperty="X"
                             Duration="0:0:0.15">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseIn" Exponent="4"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Name="SlideInStoryboard">
            <DoubleAnimation Storyboard.TargetName="TextTransform"
                             Storyboard.TargetProperty="X"
                             To="0"
                             Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="4"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="5" Grid.RowSpan="2"/>
        <Grid Padding="10 5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <FontIcon Glyph="&#xE754;"/>

            <TextBlock Grid.Column="2" l:Uids.Uid="FlashlightTipsWidgetTitleTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}"  MaxLines="1" VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" Grid.Column="4" Spacing="5" VerticalAlignment="Center">
                <Grid CornerRadius="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="1"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="PreviousTipButton" l:Uids.Uid="PreviousTipButton" Padding="7 7 6 6" CornerRadius="0" BorderThickness="0" Click="PreviousTipButton_Click">
                        <FontIcon Glyph="&#xE76B;" FontSize="16"/>
                    </Button>
                    <Rectangle Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Grid.Column="1"/>
                    <Button x:Name="NextTipButton" l:Uids.Uid="NextTipButton" Padding="7 7 6 6" Grid.Column="2" CornerRadius="0" BorderThickness="0" Click="NextTipButton_Click">
                        <FontIcon Glyph="&#xE76C;" FontSize="16"/>
                    </Button>
                </Grid>
                <Button x:Name="HideFlashlightWidgetButton" l:Uids.Uid="HideFlashlightWidgetButton" Padding="7 7 6 6" BorderThickness="0" Click="HideFlashlightWidgetButton_Click">
                    <FontIcon Glyph="&#xE711;" FontSize="16"/>
                </Button>
            </StackPanel>
        </Grid>


        <Grid MinHeight="50" Grid.Row="1">
            <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="5"/>
            <Grid Grid.Column="2" VerticalAlignment="Center"  Padding="10 ">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="TipTitleTextBlock" FontStyle="Italic" Grid.Row="0" MaxLines="1" VerticalAlignment="Center"/>
                <Grid Grid.Row="1" VerticalAlignment="Center" CornerRadius="5">
                    <TextBlock x:Name="TipDecriptionTextBlock" TextWrapping="Wrap" VerticalAlignment="Center">
                        <TextBlock.RenderTransform>
                            <TranslateTransform x:Name="TextTransform" X="0"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
