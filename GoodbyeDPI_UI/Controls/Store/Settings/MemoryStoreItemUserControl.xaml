<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPI_UI.Controls.Store.Settings.MemoryStoreItemUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Controls.Store.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls">

    <Grid>
        <Border 
            x:Name="Button" 
            HorizontalAlignment="Stretch"
            Padding="0"
            Background="{ThemeResource ControlFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="5">
            <Grid>
                <Grid Padding="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="0.5*"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <Grid Height="35" Width="35">
                        <Rectangle x:Name="Rectangle"
                            Grid.Column="0"
                            RadiusX="5" RadiusY="5"
                            HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Collapsed"/>
                        <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0"
                            BorderThickness="1" CornerRadius="5" 
                            BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}" />
                        <Image x:Name="PART_Image" Grid.Column="0" Source="{x:Bind CardImageSource, Mode=OneWay}" Width="25"/>
                    </Grid>
                    <StackPanel Grid.Column="2" VerticalAlignment="Center" >
                        <TextBlock Text="{x:Bind Title, Mode=OneWay}" MaxLines="2" TextWrapping="Wrap"/>
                        <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="8">
                            <TextBlock Text="{x:Bind Developer, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="NoWrap"/>
                            <Rectangle VerticalAlignment="Stretch" Fill="{ThemeResource TextFillColorPrimary}" Width="1" Margin="0 2"/>
                            <TextBlock x:Name="VersionTextBlock" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="NoWrap" />
                            <Rectangle VerticalAlignment="Stretch" Fill="{ThemeResource TextFillColorPrimary}" Width="1" Margin="0 2"/>
                            <TextBlock x:Name="CategoryTextBlock" Text="{x:Bind Category, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="NoWrap" />
                        </controls:WrapPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="5" Orientation="Horizontal" VerticalAlignment="Center" Spacing="10">
                        <TextBlock x:Name="SizeTextBlock" VerticalAlignment="Center"/>
                        <Button l:Uids.Uid="ViewMoreActionsButton" Padding="6" VerticalAlignment="Stretch" Background="Transparent" BorderBrush="Transparent" Height="50">
                            <FontIcon Glyph="&#xE712;" FontSize="16"/>
                            <Button.Flyout>
                                <MenuFlyout Placement="BottomEdgeAlignedRight">
                                    <MenuFlyoutItem l:Uids.Uid="DeleteButton" x:Name="DeleteButton"  Click="DeleteButton_Click">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE74D;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutItem l:Uids.Uid="SetupMenuFlyuoutItem"
                                                x:Name="SetupMenuFlyuoutItem" 
                                                Click="SetupMenuFlyuoutItem_Click">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE713;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutItem l:Uids.Uid="StoreShowItemFolder"
                                                x:Name="ShowFolderButton" 
                                                Click="ShowFolderButton_Click">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE838;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>
                    </StackPanel>
                </Grid>
                <ProgressBar x:Name="ProgressBar" IsIndeterminate="True" Grid.Row="3" Grid.ColumnSpan="6" VerticalAlignment="Bottom" CornerRadius="0" Visibility="Collapsed"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
