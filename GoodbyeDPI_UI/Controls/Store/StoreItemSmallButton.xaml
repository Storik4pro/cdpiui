<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Name="RootControl"
    x:Class="CDPI_UI.StoreItemSmallButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    MinWidth="200" MinHeight="125"
    >

    <UserControl.Resources>
        <Style x:Key="StoreSmallItemButton" TargetType="Button">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultSolidBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="PART_ShadowHost" RenderTransformOrigin="0.5,0.5">
        <Grid.RenderTransform>
            <TranslateTransform x:Name="PART_Translate" />
        </Grid.RenderTransform>
        <Grid.Resources>
            <ThemeShadow x:Name="SharedShadow" />
        </Grid.Resources>
        <Grid x:Name="BackgroundGrid" Background="Transparent" />
        
        <Button x:Name="PART_Button" 
                MinWidth="200" MinHeight="110" 
                Height="110"
                MaxHeight="110"
                VerticalAlignment="Top"
                Style="{StaticResource StoreSmallItemButton}"
                PointerEntered="OnButtonPointerEntered" PointerExited="OnButtonPointerExited"
                Shadow="{StaticResource SharedShadow}"
                Click="PART_Button_Click">
            <Button.Content>
                <Grid>
                    <Rectangle x:Name="PART_BottomRect" Fill="{ThemeResource SubtleFillColorSecondaryBrush}"
                               HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                    <Grid Padding="15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="25"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Rectangle Grid.Column="0" Fill="{x:Bind CardBackgroundBrush}" 
                               RadiusX="5" RadiusY="5"
                               HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                        <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0"
                            BorderThickness="1" CornerRadius="5" 
                            BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}"/>
                        <Image x:Name="PART_Image" Grid.Column="0" Source="{x:Bind CardImageSource}" Width="60"/>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="{x:Bind CardTitle}" 
                                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                                   MaxLines="3"/>
                            <TextBlock Text="{x:Bind CardDeveloper}" Margin="0, 5, 0, 0" 
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <Grid Grid.Column="2" HorizontalAlignment="Right" Padding="0" VerticalAlignment="Bottom">
                            <Rectangle Fill="{ThemeResource SubtleFillColorSecondaryBrush}" RadiusX="3" RadiusY="3"/>
                            <TextBlock Text="{x:Bind CardPrice}" Style="{ThemeResource CaptionTextBlockStyle}" Margin="8, 2"/>
                        </Grid>
                    </Grid>
                </Grid>
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
