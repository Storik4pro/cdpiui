<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GoodbyeDPI_UI.StoreItemMicroButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoodbyeDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI">

    <UserControl.Resources>
        <Style x:Key="StoreLargeButtonStyle" TargetType="Button">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultSolidBrush}"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Button x:Name="PART_Button" 
                HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" 
                MinWidth="100" MinHeight="100"
                MaxWidth="100"
                Style="{StaticResource StoreLargeButtonStyle}" 
                PointerEntered="PART_Button_PointerEntered"  PointerExited="PART_Button_PointerExited"
                Click="PART_Button_Click">
            <Button.Content>
                <Grid CornerRadius="10">
                    <Grid Width="35" Height="35" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Image x:Name="PART_Image" Source="{x:Bind CardImageSource}" />
                        <labs:Shimmer x:Name="ImageShimmer" Visibility="Visible" CornerRadius="0"/>
                    </Grid>
                    <Grid VerticalAlignment="Bottom">
                        <Grid x:Name="TextGrid">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{x:Bind CardTitle}" Style="{ThemeResource BodyStrongTextBlockStyle}" 
                                   Margin="5 0 5 5" TextWrapping="NoWrap"/>
                            <Grid HorizontalAlignment="Right" Margin="0 0 5 5" Grid.Column="1">
                                <FontIcon x:Name="InstallationFontIcon" Glyph="&#xEBD3;" FontSize="16" />
                            </Grid>
                        </Grid>
                        <labs:Shimmer x:Name="TextShimmer" Visibility="Visible" Margin="5 0 5 5" Height="15"/>
                    </Grid>
                    <ProgressBar x:Name="ProgressBar" IsIndeterminate="True" VerticalAlignment="Bottom" Visibility="Collapsed"/>
                </Grid>
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
