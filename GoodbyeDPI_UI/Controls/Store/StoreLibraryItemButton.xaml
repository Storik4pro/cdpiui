<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPI_UI.Controls.Store.StoreLibraryItemButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Controls.Store"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer">

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="SupeSState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ActionButton.Visibility" Value="Collapsed" />
                        <Setter Target="CategoryTextBlock.Visibility" Value="Collapsed" />
                        <Setter Target="BigActionButton.Visibility" Value="Visible" />
                        <Setter Target="BigCategoryTextBlock.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="SmallState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="600"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ActionButton.Visibility" Value="Visible" />
                        <Setter Target="CategoryTextBlock.Visibility" Value="Visible" />
                        <Setter Target="BigActionButton.Visibility" Value="Collapsed" />
                        <Setter Target="BigCategoryTextBlock.Visibility" Value="Collapsed" />
                        <Setter Target="CategoryTextBlock.MinWidth" Value="80" />
                        <Setter Target="CategoryTextBlock.Width" Value="80" />
                        
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="800"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ActionButton.Visibility" Value="Visible" />
                        <Setter Target="CategoryTextBlock.Visibility" Value="Visible" />
                        <Setter Target="BigActionButton.Visibility" Value="Collapsed" />
                        <Setter Target="BigCategoryTextBlock.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
                
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Button x:Name="Button" 
                Click="Button_Click"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                Padding="0">
            <Grid>
                <Grid Padding="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="0.5*"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <Grid Height="60" Width="60">
                        <Rectangle x:Name="Rectangle"
                               Grid.Column="0"
                               RadiusX="5" RadiusY="5"
                               HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                        <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0"
                            BorderThickness="1" CornerRadius="5" 
                            BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}"/>
                        <Image x:Name="PART_Image" Grid.Column="0" Source="{x:Bind CardImageSource, Mode=OneWay}" Width="40"/>
                    </Grid>
                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                        <TextBlock Text="{x:Bind Title, Mode=OneWay}" MaxLines="2" TextWrapping="Wrap"/>
                        <TextBlock Text="{x:Bind Developer, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="NoWrap"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3" VerticalAlignment="Center">
                        <TextBlock x:Name="CategoryTextBlock" Text="{x:Bind Category, Mode=OneWay}" MinWidth="150" TextWrapping="Wrap"  />
                    </StackPanel>
                    <StackPanel Grid.Column="4" VerticalAlignment="Center">
                        <TextBlock x:Name="VersionTextBlock" MinWidth="50"  />
                    </StackPanel>
                    <StackPanel Grid.Column="5" Orientation="Horizontal" VerticalAlignment="Center" Spacing="10">
                        <Button l:Uids.Uid="SetupButton" x:Name="ActionButton" MinWidth="125" Click="ActionButton_Click"/>
                        <Button l:Uids.Uid="ViewMoreActionsButton" Padding="6" VerticalAlignment="Stretch" Background="Transparent" BorderBrush="Transparent">
                            <FontIcon Glyph="&#xE712;" FontSize="16"/>
                            <Button.Flyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem l:Uids.Uid="DeleteButton" x:Name="DeleteButton"  Click="DeleteButton_Click">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE74D;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutItem l:Uids.Uid="ReinstallButton"
                                                    x:Name="ReinstallButton" 
                                                    Click="ReinstallButton_Click">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE895;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutItem l:Uids.Uid="StoreShowItemFolder"
                                                    x:Name="ShowFolderButton" 
                                                    Click="ShowFolderButton_Click">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE838;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>
                    </StackPanel>
                    <Button l:Uids.Uid="SetupButton" x:Name="BigActionButton" Grid.Row="1" HorizontalAlignment="Stretch" Grid.ColumnSpan="6" Margin="0 5" Visibility="Collapsed" Click="ActionButton_Click"/>
                    <TextBlock x:Name="BigCategoryTextBlock" Grid.Row="2" Grid.ColumnSpan="6" Text="{x:Bind Category, Mode=OneWay}" Visibility="Collapsed"/>
                </Grid>
                <ProgressBar x:Name="ProgressBar" IsIndeterminate="True" Grid.Row="3" Grid.ColumnSpan="6" VerticalAlignment="Bottom" CornerRadius="0" Visibility="Collapsed"/>
            </Grid>
        </Button>
    </Grid>
</UserControl>
