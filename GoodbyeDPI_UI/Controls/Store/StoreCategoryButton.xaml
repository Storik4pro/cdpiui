<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPI_UI.StoreCategoryButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="StoreCategoryButtonStyle" TargetType="Button">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid 
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}">
                            <ContentPresenter x:Name="ContentPresenter" 
                                              BorderBrush="{TemplateBinding BorderBrush}" 
                                              BorderThickness="{TemplateBinding BorderThickness}" 
                                              Content="{TemplateBinding Content}" 
                                              ContentTransitions="{TemplateBinding ContentTransitions}" 
                                              ContentTemplate="{TemplateBinding ContentTemplate}" 
                                              Padding="{TemplateBinding Padding}" 
                                              HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" 
                                              AutomationProperties.AccessibilityView="Raw">
                                
                            </ContentPresenter>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            
        </Style>
        <Storyboard x:Name="AnimToSecondary">
            <ColorAnimation
                Storyboard.TargetName="TextBlockForegroundBrush"
                Storyboard.TargetProperty="Color"
                To="{StaticResource SystemAccentColorLight3}"
                Duration="0:0:0.2" />
            <DoubleAnimation Storyboard.TargetName="ContentTranslateTransform"
                 Storyboard.TargetProperty="X"
                 To="2"
                 From="-10"
                 Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase Exponent="4"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            
        </Storyboard>

        <Storyboard x:Name="AnimToPrimary">
            <ColorAnimation
                Storyboard.TargetName="TextBlockForegroundBrush"
                Storyboard.TargetProperty="Color"
                To="{StaticResource TextFillColorPrimary}"
                Duration="0:0:0.2" />
            <DoubleAnimation Storyboard.TargetName="ContentTranslateTransform"
                 Storyboard.TargetProperty="X"
                 To="-10"
                 From="2"
                 Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase Exponent="4"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            
        </Storyboard>

    </UserControl.Resources>


    <Grid HorizontalAlignment="Left" PointerEntered="Button_PointerEntered" PointerExited="Button_PointerExited" Margin="0, 0, 0, 10">
        <Rectangle x:Name="PART_BackgroundRect" Fill="{ThemeResource ControlStrokeColorOnAccentDefaultBrush}" HorizontalAlignment="Stretch" RadiusX="10" RadiusY="10"/>
        <Button x:Name="PART_Button" Style="{StaticResource StoreCategoryButtonStyle}" Padding="11,4,11,5" Click="PART_Button_Click">
            <Button.Content>
                <StackPanel Orientation="Horizontal">
                    <StackPanel.RenderTransform>
                        <TranslateTransform x:Name="ContentTranslateTransform"/>
                    </StackPanel.RenderTransform>
                    <TextBlock 
                        x:Name="MainTextBlock"
                        Text="{x:Bind Text}" 
                        FontWeight="SemiBold"
                        Style="{ThemeResource SubtitleTextBlockStyle}">
                        <TextBlock.Foreground>
                            <SolidColorBrush x:Name="TextBlockForegroundBrush" Color="{ThemeResource TextFillColorPrimary}"/>
                        </TextBlock.Foreground>
                    </TextBlock>
                    <FontIcon 
                        x:Name="FontIcon"
                        Glyph="&#xE76C;"
                        FontSize="15" FontWeight="Black"
                        VerticalAlignment="Bottom" Margin="0,0,0,3"
                        Foreground="{ThemeResource AccentAAFillColorSecondaryBrush}"/>
                </StackPanel>
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
