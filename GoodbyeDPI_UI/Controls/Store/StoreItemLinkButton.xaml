<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GoodbyeDPI_UI.StoreItemLinkButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoodbyeDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    HorizontalAlignment="Stretch"
    Margin="-1, 0, 0, 0">

    <Grid>
        <Rectangle Fill="{ThemeResource ControlElevationBorderBrush}" Height="2" HorizontalAlignment="Stretch" VerticalAlignment="Top"/>
        <Button 
            x:Name="Button"
            HorizontalAlignment="Stretch" 
            CornerRadius="0" 
            Background="Transparent" 
            BorderBrush="Transparent"
            HorizontalContentAlignment="Stretch"
            Padding="0"
            Height="60"
            Click="Button_Click">
            <Button.Content>
                <Grid Margin="20, 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Glyph="{x:Bind SiteIcon}" FontSize="16" Margin="0, 0, 10, 0" />
                    <TextBlock Grid.Column="1" Text="{x:Bind Text}" VerticalAlignment="Center" MaxLines="1"/>
                    <Button Grid.Column="2" Padding="8" Background="Transparent" BorderBrush="Transparent" Click="Button_Click">
                        <Button.Content>
                            <FontIcon Glyph="&#xE8A7;" FontSize="16" />
                        </Button.Content>
                    </Button>
                </Grid>
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
