<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GoodbyeDPI_UI.SitelistButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoodbyeDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Button Padding="6" HorizontalContentAlignment="Stretch" HorizontalAlignment="Stretch" Height="40" Click="Button_Click">
            <Button.Content>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Glyph="&#xE7C3;" VerticalAlignment="Center" Margin="0 0 10 0"/>
                    <TextBlock Text="{x:Bind Title}" VerticalAlignment="Center" Grid.Column="1" Margin="0 0 10 0"/>
                    <TextBlock Text="{x:Bind PackName}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Grid.Column="3" TextAlignment="Right"/>
                </Grid>
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
