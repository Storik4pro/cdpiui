<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPI_UI.IconButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="TextDefaultColor" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimary}"/>
        </Style>
        <Style x:Key="FontIconDefaultColor" TargetType="FontIcon">
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimary}"/>
        </Style>
        <Style x:Key="TextAccentColor" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
        </Style>
        <Style x:Key="FontIconAccentColor" TargetType="FontIcon">
            <Setter Property="Foreground" Value="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
        </Style>
        <Style x:Key="TextAccentTertiaryColor" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{ThemeResource AccentTextFillColorTertiaryBrush}"/>
        </Style>
        <Style x:Key="FontIconAccentTertiaryColor" TargetType="FontIcon">
            <Setter Property="Foreground" Value="{ThemeResource AccentTextFillColorTertiaryBrush}"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Button Padding="6" Click="Button_Click" PointerEntered="Button_PointerEntered" PointerExited="Button_PointerExited" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
            <Button.Resources>
                <SolidColorBrush x:Key="ButtonBackground" Color="Transparent"/>
                <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="Transparent"/>
                <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="Transparent"/>
                <SolidColorBrush x:Key="ButtonBackgroundDisabled" Color="Transparent"/>
                <SolidColorBrush x:Key="ButtonBorderBrush" Color="Transparent"/>
                <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="Transparent"/>
                <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="Transparent"/>
                <SolidColorBrush x:Key="ButtonBorderBrushDisabled" Color="Transparent"/>
            </Button.Resources>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <FontIcon x:Name="Icon" FontSize="16"/>
                <TextBlock x:Name="MainTextBlock" Visibility="Collapsed"/>
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
