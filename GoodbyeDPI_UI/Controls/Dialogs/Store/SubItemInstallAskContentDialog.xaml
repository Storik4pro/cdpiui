<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    l:Uids.Uid="SimilarItemInstallingContentDialog"
    x:Class="CDPI_UI.Controls.Dialogs.Store.SubItemInstallAskContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Controls.Dialogs.Store"
    xmlns:lcontols="using:CDPI_UI.Controls.Store"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer"
    Style="{ThemeResource DefaultContentDialogStyle}"
    PrimaryButtonText="OK">

    <Grid Width="400">
        <ListView x:Name="ItemsListView" Margin="0 5 0 0" SelectionMode="None" Padding="0"
                  ItemContainerStyle="{StaticResource ListViewItemStretchStyle}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:SubItemModel">
                    <lcontols:StoreAskForSubItemInstallUserControl
                        StoreId="{x:Bind Id, Mode=OneWay}"
                        CardImageSource="{x:Bind ImageSource, Mode=OneWay}"
                        CardTitle="{x:Bind Name, Mode=OneWay}"
                        CardCategory="{x:Bind Category, Mode=OneWay}"
                        Developer="{x:Bind Developer, Mode=OneWay}"
                        Description="{x:Bind Description, Mode=OneWay}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Spacing="10"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
        
    </Grid>
</ContentDialog>
