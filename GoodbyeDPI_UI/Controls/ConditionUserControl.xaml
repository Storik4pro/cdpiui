<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GoodbyeDPI_UI.ConditionUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoodbyeDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="0 2.5">
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="5"/>
        <VariableSizedWrapGrid ItemWidth="300" ItemHeight="45" Orientation="Horizontal" Margin="10">

            <StackPanel x:Name="VariableNameStackPanel" VerticalAlignment="Center" ToolTipService.ToolTip="{x:Bind VarName, Mode=OneWay}">
                <TextBlock Text="Name" Style="{ThemeResource CaptionTextBlockStyle}"/>
                <TextBlock Text="{x:Bind VarName, Mode=OneWay}" IsTextSelectionEnabled="True"/>
            </StackPanel>
            <StackPanel x:Name="VariableOnStackPanel" VerticalAlignment="Center" ToolTipService.ToolTip="{x:Bind OnValue, Mode=OneWay}">
                <TextBlock Text="OnValue" Style="{ThemeResource CaptionTextBlockStyle}"/>
                <TextBlock Text="{x:Bind OnValue, Mode=OneWay}" IsTextSelectionEnabled="True"/>
            </StackPanel>
            <StackPanel x:Name="VariableOffStackPanel" VerticalAlignment="Center" ToolTipService.ToolTip="{x:Bind OffValue, Mode=OneWay}">
                <TextBlock Text="OffValue" Style="{ThemeResource CaptionTextBlockStyle}"/>
                <TextBlock Text="{x:Bind OffValue, Mode=OneWay}" IsTextSelectionEnabled="True"/>
            </StackPanel>
            <StackPanel x:Name="VariableDescriptionStackPanel" VerticalAlignment="Center" ToolTipService.ToolTip="{x:Bind Description, Mode=OneWay}">
                <TextBlock Text="Description" Style="{ThemeResource CaptionTextBlockStyle}"/>
                <TextBlock Text="{x:Bind Description, Mode=OneWay}" IsTextSelectionEnabled="True"/>
            </StackPanel>
        </VariableSizedWrapGrid>
        <StackPanel Grid.Column="5" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10 0">
            <Button Padding="6" ToolTipService.ToolTip="Remove" Click="Button_Click">
                <Button.Content>
                    <FontIcon Glyph="&#xE711;" FontSize="16" />
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
