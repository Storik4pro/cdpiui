<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPI_UI.VariableUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Margin="0 2.5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.ColumnSpan="3" Grid.RowSpan="2"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="5" />

        <StackPanel x:Name="VariableNameStackPanel"
                    Grid.Row="0" Grid.Column="0"
                    VerticalAlignment="Center"
                    Margin="10,10,6,10"
                    ToolTipService.ToolTip="{x:Bind VarName, Mode=OneWay}">
            <TextBlock Text="Name" Style="{ThemeResource CaptionTextBlockStyle}"/>
            <TextBlock Text="{x:Bind VarName, Mode=OneWay}" IsTextSelectionEnabled="True"/>
        </StackPanel>

        <StackPanel x:Name="VariableValueStackPanel"
                    Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                    VerticalAlignment="Center"
                    Margin="10,10,10,10"
                    ToolTipService.ToolTip="{x:Bind VarValue, Mode=OneWay}">
            <TextBlock Text="Value" Style="{ThemeResource CaptionTextBlockStyle}"/>
            <TextBlock Text="{x:Bind VarValue, Mode=OneWay}"
                   IsTextSelectionEnabled="True"/>
        </StackPanel>

        <StackPanel x:Name="ActionsPanel"
                    Grid.Row="0" Grid.Column="2"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="10,0"
                    Orientation="Horizontal"
                    Spacing="10">
            <Button x:Name="EditButton" Padding="6" Click="EditButton_Click">
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <FontIcon Glyph="&#xE70F;" FontSize="16" />
                    <TextBlock Text="Edit value"/>
                </StackPanel>
            </Button>
            <Button Padding="6" ToolTipService.ToolTip="Remove" Height="33" Width="33">
                <FontIcon Glyph="&#xE711;" FontSize="16" />
                <Button.Flyout>
                    <Flyout x:Name="RemoveFlyout">
                        <StackPanel Width="300">
                            <TextBlock Text="Are you shure?" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                            <TextBlock Text="This action cannot be undo."/>
                            <StackPanel Orientation="Horizontal" Spacing="10" Margin="0 10 0 0">
                                <Button x:Name="RemoveButton" Content="Yes, delete" HorizontalAlignment="Stretch" Click="RemoveButton_Click"/>
                                <Button x:Name="CloseFlyout" 
                                        Content="No, cancel" Style="{ThemeResource AccentButtonStyle}" 
                                        HorizontalAlignment="Stretch" Click="CloseFlyout_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
            </Button>
        </StackPanel>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="600"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="VariableNameStackPanel.(Grid.Row)" Value="0"/>
                        <Setter Target="VariableNameStackPanel.(Grid.Column)" Value="0"/>
                        <Setter Target="VariableNameStackPanel.(Grid.ColumnSpan)" Value="1"/>

                        <Setter Target="VariableValueStackPanel.(Grid.Row)" Value="0"/>
                        <Setter Target="VariableValueStackPanel.(Grid.Column)" Value="1"/>
                        <Setter Target="VariableValueStackPanel.(Grid.ColumnSpan)" Value="1"/>

                        <Setter Target="ActionsPanel.(Grid.Row)" Value="0"/>
                        <Setter Target="ActionsPanel.(Grid.Column)" Value="2"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</UserControl>
