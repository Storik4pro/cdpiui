<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Name="RootControl"
    x:Class="CDPI_UI.CreateConfigDesignerUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI"
    xmlns:lcontrols="using:CDPI_UI.Controls.Dialogs.CreateConfigHelper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer"
    Height="60"
    MaxHeight="60">

    <Grid>
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="5"/>
        <Grid Padding="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid x:Name="ControlsGrid" Margin="0 0 10 0">
                <BitmapIcon UriSource="ms-appx:///Assets/Icons/DragReorder.png" Width="25"/>
            </Grid>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="0">
                    <StackPanel Orientation="Horizontal" x:Name="ModeStackPanel" Margin="0 -5 0 0">
                        <ListView x:Name="BadgesListView"
                                  ItemContainerStyle="{StaticResource ListViewSmallItemStretchStyle}"
                                  Grid.Row="1"
                                  BorderThickness="1"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsStackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="lcontrols:ByeDPIModeComboBoxItem">
                                    <Grid CornerRadius="5" ToolTipService.ToolTip="{x:Bind DisplayTip, Mode=OneWay}" Margin="0 0 5 0">
                                        <Rectangle Fill="{ThemeResource SystemFillColorAttentionBrush}"/>
                                        <TextBlock Text="{x:Bind DisplayName, Mode=OneWay}" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" Margin="3 0" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        
                    </StackPanel>
                    <TextBlock Text="{x:Bind SiteListName, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="NoWrap" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="{x:Bind Args, Mode=OneWay}"/>
            </StackPanel>
            <StackPanel x:Name="ControlsStackPanel" Orientation="Horizontal" HorizontalAlignment="Right" Visibility="Visible" Grid.Column="2">
                <local:IconButton x:Name="EditMode" l:Uids.Uid="EditByeDPIModeButton" IconGlyph="&#xE70F;"
                                  ClickCommand="{Binding EditModeCommand, ElementName=RootControl}" Visibility="Collapsed"/>
                <local:IconButton x:Name="ViewIconButton" l:Uids.Uid="ViewFullStrategyButton" IconGlyph="&#xE890;" 
                      ClickCommand="{Binding ViewFullCommand, ElementName=RootControl}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
