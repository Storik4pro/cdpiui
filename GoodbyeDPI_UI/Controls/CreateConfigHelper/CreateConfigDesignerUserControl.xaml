<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Name="RootControl"
    x:Class="GoodbyeDPI_UI.CreateConfigDesignerUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoodbyeDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="5"/>
        <Grid Padding="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid x:Name="ControlsGrid" Margin="0 0 10 0">
                <BitmapIcon UriSource="ms-appx:///Assets/Icons/DragReorder.png" Width="25"/>
            </Grid>
            <StackPanel Grid.Column="1">
                <TextBlock Text="{x:Bind SiteListName, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="NoWrap"/>
                <TextBlock Text="{x:Bind Args, Mode=OneWay}"/>
            </StackPanel>
            <StackPanel x:Name="ControlsStackPanel" Orientation="Horizontal" HorizontalAlignment="Right" Visibility="Visible" Grid.Column="2">
                <local:IconButton x:Name="ViewIconButton" IconGlyph="&#xE890;" ToolTipService.ToolTip="View full strategy string" 
                      ClickCommand="{Binding ViewFullCommand, ElementName=RootControl}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
