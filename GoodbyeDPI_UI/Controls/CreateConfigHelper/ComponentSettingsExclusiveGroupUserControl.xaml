<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Name="RootControl"
    x:Class="CDPI_UI.Controls.CreateConfigHelper.ComponentSettingsExclusiveGroupUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Controls.CreateConfigHelper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:lcontrols="using:CDPI_UI"
    xmlns:l="using:WinUI3Localizer">

    <UserControl.Resources>
        <Style TargetType="SelectorBarItem">
            <Setter Property="Padding" Value="10 0" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Background="{ThemeResource SystemChromeBlackLowColor}" Grid.RowSpan="3" CornerRadius="6" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"/>
        <Grid Padding="10 10 10 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock l:Uids.Uid="ExclusiveGroupTextBlock"  Style="{ThemeResource BodyStrongTextBlockStyle}" VerticalAlignment="Top"/>
            <SelectorBar Grid.Column="2" x:Name="SelectorBar" SelectionChanged="SelectorBar_SelectionChanged" Padding="0"/>

        </Grid>
        <lcontrols:ComponentSettingUserControl Grid.Row="2" 
                                               x:Name="MainComponentSettingsUserControl"
                                               ValueChangedCommand="{Binding GraphicDesignerTextValueChangedCommand, ElementName=RootControl}"
                                               ToggledCommand="{Binding GraphicDesignerBoolValueToggledCommand, ElementName=RootControl}"
                                               Visibility="Collapsed"/>
    </Grid>
</UserControl>
