<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    l:Uids.Uid="SelectConfigKitToEditContentDialog"
    x:Class="CDPI_UI.Controls.CreateConfigHelper.SelectConfigKitToEditContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Controls.CreateConfigHelper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
    Style="{ThemeResource DefaultContentDialogStyle}"
    xmlns:viewmodels="using:CDPI_UI.ViewModels">

    <Grid HorizontalAlignment="Stretch" Width="400">
        <ListView
            x:Name="ConfigKitListView"
            ItemContainerStyle="{StaticResource ListViewSmallItemStretchStyle}"
            Margin="0 0 -15 0">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:ViewStoreItemModel">
                    <Button Padding="6" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Margin="0 0 15 0" Tag="{x:Bind StoreId, Mode=OneWay}" Click="Button_Click">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0" Height="50" Width="50" CornerRadius="5">
                                <Rectangle Fill="{ThemeResource SystemAccentColor}"/>
                                <Rectangle>
                                    <Rectangle.Fill>
                                        <SolidColorBrush Color="{x:Bind Color, Mode=OneWay}"/>
                                    </Rectangle.Fill>
                                </Rectangle>
                                <Image Source="{x:Bind ImageSource, Mode=OneWay}" Width="40"/>
                            </Grid>
                            <Grid Grid.Column="2" VerticalAlignment="Center">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{x:Bind Name, Mode=OneWay}"/>
                                <TextBlock Grid.Row="1" Text="{x:Bind Developer, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondary}"/>
                            </Grid>
                            
                        </Grid>
                    </Button>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Spacing="10"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </Grid>
</ContentDialog>
