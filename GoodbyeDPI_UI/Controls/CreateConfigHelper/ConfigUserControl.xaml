<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPI_UI.Controls.CreateConfigHelper.ConfigUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Controls.CreateConfigHelper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:l="using:WinUI3Localizer">

    <Grid>
        <Border Background="{ThemeResource ControlFillColorDefaultBrush}" BorderBrush="{ThemeResource ControlElevationBorderBrush}" BorderThickness="1" CornerRadius="5"/>
        <Grid Padding="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{x:Bind DisplayName, Mode=OneWay}" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                <controls:WrapPanel Grid.Row="1" Orientation="Horizontal" HorizontalSpacing="8">
                    <TextBlock x:Name="ComponentNameTextBlock" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="NoWrap"/>
                    <Rectangle VerticalAlignment="Stretch" Fill="{ThemeResource TextFillColorPrimary}" Width="1" Margin="0 2"/>
                    <TextBlock x:Name="SitelistTextBlock" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="NoWrap" />
                    <Rectangle VerticalAlignment="Stretch" Fill="{ThemeResource TextFillColorPrimary}" Width="1" Margin="0 2"/>
                    <TextBlock x:Name="ExcludedSitelistTextBlock" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="NoWrap" />
                    <Rectangle VerticalAlignment="Stretch" Fill="{ThemeResource TextFillColorPrimary}" Width="1" Margin="0 2"/>
                    <TextBlock x:Name="LastEditTextBlock" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="NoWrap" />
                </controls:WrapPanel>
            </Grid>
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="5">
                <Button x:Name="RenameButton" l:Uids.Uid="RenameButton" Padding="6">
                    <FontIcon Glyph="&#xE8AC;" FontSize="16"/>
                    <Button.Flyout>
                        <Flyout x:Name="RenameFlyout" Placement="BottomEdgeAlignedRight" Closed="Flyout_Closed">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="10"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock l:Uids.Uid="RenameTitleTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                <TextBox x:Name="RenameTextBox" Grid.Row="1" Width="300" TextChanged="RenameTextBox_TextChanged"/>
                                <Button Grid.Row="3" l:Uids.Uid="ApplyRenameButton" x:Name="ApplyRenameButton" Click="ApplyRenameButton_Click" Style="{ThemeResource AccentButtonStyle}" MinWidth="150" HorizontalAlignment="Right"/>
                            </Grid>
                        </Flyout>
                    </Button.Flyout>
                </Button>
                <Button x:Name="EditButton" l:Uids.Uid="EditButton" Padding="6" Click="EditButton_Click">
                    <FontIcon Glyph="&#xE70F;" FontSize="16"/>
                </Button>
                <Button Padding="6" l:Uids.Uid="ViewUsedDirectoriesButton" Click="Button_Click" Visibility="Collapsed">
                    <FontIcon Glyph="&#xED25;" FontSize="16"/>
                    
                </Button>
                <Button x:Name="RemoveButton" l:Uids.Uid="RemoveButton" Padding="6" Click="RemoveButton_Click">
                    <FontIcon Glyph="&#xE74D;" FontSize="16"/>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
