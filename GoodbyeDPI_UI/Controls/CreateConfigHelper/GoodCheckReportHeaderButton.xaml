<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Name="RootControl"
    x:Class="GoodbyeDPI_UI.GoodCheckReportHeaderButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoodbyeDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    CornerRadius="5">

    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Padding="0" Click="Button_Click" CornerRadius="{Binding CornerRadius, ElementName=RootControl}">
        <Grid x:Name="ContentGrid" Padding="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <FontIcon Glyph="&#xE9F9;" />
            <StackPanel Grid.Column="2">
                <Grid>
                    <TextBlock Text="{x:Bind SubHeader, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                    <TextBlock Text="{x:Bind Header, Mode=OneWay}" Style="{ThemeResource BodyStrongTextBlockStyle}" Margin="10 12 0 0"/>
                </Grid>
                <VariableSizedWrapGrid Orientation="Horizontal" ItemWidth="150" Margin="0 3 0 0">
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="30"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Spacing="5">
                            <FontIcon Glyph="&#xE930;" FontSize="16"/>
                            <TextBlock Text="Success" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{x:Bind SuccessCount, Mode=OneWay}" TextAlignment="Right"/>
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="5">
                            <FontIcon Glyph="&#xEA39;" FontSize="16"/>
                            <TextBlock Text="Failure" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Grid.Row="2"  Grid.Column="1" Text="{x:Bind FailureCount, Mode=OneWay}" TextAlignment="Right"/>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Spacing="5">
                            <FontIcon Glyph="&#xE7C1;" FontSize="16"/>
                            <TextBlock Text="Flags" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{x:Bind FlagsCount, Mode=OneWay}" TextAlignment="Right"/>
                    </Grid>
                </VariableSizedWrapGrid>
            </StackPanel>
            <StackPanel Grid.Column="3" VerticalAlignment="Center">
                <Button Padding="6" Click="Button_Click">
                    <FontIcon x:Name="ChevronFontIcon" Glyph="&#xE70D;" FontSize="16"/>
                </Button>
            </StackPanel>
        </Grid>
    </Button>
</UserControl>
