<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Name="RootPage"
    x:Class="CDPI_UI.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:BackdropMaterial.ApplyToRootOrPageBackground="True"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:l="using:WinUI3Localizer"
    xmlns:viewmodels="using:CDPI_UI.ViewModels"
    xmlns:conv="using:CDPI_UI.Converters"
    xmlns:mpcontrols="using:CDPI_UI.Controls.MainPage"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals">

    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Page.Resources>

    <Grid x:Name="RootGrid">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="SmallState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainStackPanel.HorizontalAlignment" Value="Stretch" />
                        <Setter Target="MainStackPanel.Width" Value="auto" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1100"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainStackPanel.HorizontalAlignment" Value="Center" />
                        <Setter Target="MainStackPanel.Width" Value="1060" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <ScrollViewer Padding="20 0">
            <StackPanel x:Name="MainStackPanel" Spacing="20" Margin="0 20">
                <mpcontrols:WelcomePlaceholderUserControl x:Name="ComponentTilePlaceholder"/>

                <ListView x:Name="MainListView" ItemContainerStyle="{StaticResource ListViewItemStretchStyle}" SelectionMode="None" Loaded="MainListView_Loaded">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:ViewStoreItemModel">
                            <mpcontrols:SmallComponentTileUserControl 
                                CardBackgroundColor="{x:Bind ColorHEX, Mode=OneWay}" 
                                CardImageSource="{x:Bind ImageSource, Mode=OneWay}" 
                                CardTitle="{x:Bind Name, Mode=OneWay}"
                                StoreId="{x:Bind StoreId, Mode=OneWay}"
                                IsOpened="False"
                                ShowSettingsCommand="{Binding ShowComponentSettingsClickCommand, ElementName=RootPage}"
                                ShowSettingsCommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Spacing="5"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                </ListView>
                <StackPanel Spacing="5">
                    <StackPanel.ChildrenTransitions>
                        <TransitionCollection>
                            <RepositionThemeTransition />
                        </TransitionCollection>
                    </StackPanel.ChildrenTransitions>

                    <mpcontrols:FlashlightTipsWidgetUserControl x:Name="FlashlightTipsWidgetUserControl" />
                    <mpcontrols:WidgetUserControl/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

    </Grid>

</Page>
