<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDPI_UI.Views.CreateConfigUtil.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Views.CreateConfigUtil"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer"
    xmlns:models="using:CDPI_UI.ViewModels">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Padding="20 20 20 0" Spacing="5">
            <TextBlock l:Uids.Uid="WhatYouWantToDoTextBlock" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0 0 0 10"/>
            <TextBlock l:Uids.Uid="SelectComponentToViewVarsTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
            <ComboBox x:Name="ComponentChooseComboBox" SelectionChanged="ComponentChooseComboBox_SelectionChanged">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="models:ViewComponentModel">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Image Source="{x:Bind ImageSource, Mode=OneWay}" Width="20"/>
                            <TextBlock Text="{x:Bind DisplayName, Mode=OneWay}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <VariableSizedWrapGrid Orientation="Horizontal" ItemWidth="200" VerticalAlignment="Center" Grid.Row="1" Margin="20 0">
            <StackPanel x:Name="GoodCheckSelectionPanel" Spacing="10">
                <StackPanel Grid.Column="0" Spacing="5" >
                    <TextBlock l:Uids.Uid="GoodCheckConfigSelectionTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                    <StackPanel Grid.Column="0" Spacing="10" Orientation="Horizontal">
                        <Button x:Name="BeginNewSelectionButton" 
                                Width="120" 
                                Height="120" 
                                Padding="6" 
                                VerticalContentAlignment="Stretch" 
                                HorizontalContentAlignment="Stretch" 
                                Click="BeginNewSelectionButton_Click">
                            <Button.Content>
                                <Grid>
                                    <FontIcon Glyph="&#xEB9D;" />
                                    <TextBlock l:Uids.Uid="BeginNewSelectionTextBlock" TextWrapping="Wrap" TextAlignment="Left" VerticalAlignment="Bottom" HorizontalAlignment="Stretch"/>
                                </Grid>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
            <StackPanel Spacing="10">
                <StackPanel Grid.Column="0" Spacing="5" >
                    <TextBlock l:Uids.Uid="HelpSupportTitle" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                    <StackPanel Grid.Column="0" Spacing="10" Orientation="Horizontal">
                        <Button x:Name="ViewHelpButton" Width="120" Height="120" Padding="6" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" Click="ViewHelpButton_Click">
                            <Button.Content>
                                <Grid>
                                    <FontIcon Glyph="&#xE897;" />
                                    <TextBlock l:Uids.Uid="GetHelpTextBlock" TextWrapping="Wrap" TextAlignment="Left" VerticalAlignment="Bottom" HorizontalAlignment="Stretch"/>
                                </Grid>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
            <StackPanel Spacing="10">
                <StackPanel Grid.Column="0" Spacing="5" >
                    <TextBlock l:Uids.Uid="OtherTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                    <StackPanel Grid.Column="0" Spacing="10" Orientation="Horizontal">
                        <Button x:Name="ViewOtherButton" Width="120" Height="120" Padding="6" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" Click="ViewOtherButton_Click">
                            <Button.Content>
                                <Grid>
                                    <FontIcon Glyph="&#xE8A7;" />
                                    <TextBlock l:Uids.Uid="OpenConfigHelperTextBlock" TextWrapping="Wrap" TextAlignment="Left" VerticalAlignment="Bottom" HorizontalAlignment="Stretch"/>
                                </Grid>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </VariableSizedWrapGrid>
        <StackPanel x:Name="PlaceholderGrid" Grid.Row="1" Margin="20 20 20 0" Spacing="20">
            <TextBlock l:Uids.Uid="AutoSelectionNotAvailableTextBlock" TextWrapping="Wrap"/>
            <HyperlinkButton Click="ViewOtherButton_Click">
                <TextBlock l:Uids.Uid="OpenConfigHelperTextBlock"/>
            </HyperlinkButton>
        </StackPanel>
        <Grid x:Name="ActionButtonsGrid" Grid.Row="2">
            <Rectangle Fill="{ThemeResource ControlFillColorSecondaryBrush}"/>

            <Grid Padding="20">
                <Button x:Name="GetHelpButton" l:Uids.Uid="GetHelpButton" Padding="6" Click="GetHelpButton_Click">
                    <Button.Content>
                        <FontIcon Glyph="&#xE897;" FontSize="16"/>
                    </Button.Content>
                </Button>
                <StackPanel Orientation="Horizontal" Spacing="10"  HorizontalAlignment="Right">
                    <Button x:Name="CancelButton" l:Uids.Uid="ExitButton" MinWidth="150" Click="CancelButton_Click" IsEnabled="True"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
