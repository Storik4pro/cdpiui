<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDPI_UI.Views.CreateConfigUtil.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Views.CreateConfigUtil"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer"
    xmlns:models="using:CDPI_UI.ViewModels"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Padding="20 20 20 0" Spacing="5" x:Name="WelcomePanel">
            <TextBlock l:Uids.Uid="WhatYouWantToDoTextBlock" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0 0 0 10"/>
            <TextBlock l:Uids.Uid="SelectComponentToViewVarsTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
            <ComboBox x:Name="ComponentChooseComboBox" SelectionChanged="ComponentChooseComboBox_SelectionChanged" MinWidth="300">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="models:ViewComponentModel">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Image Source="{x:Bind ImageSource, Mode=OneWay}" Width="20"/>
                            <TextBlock Text="{x:Bind DisplayName, Mode=OneWay}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="1" Padding="20" Spacing="5" x:Name="MainPanel">
            <StackPanel.ChildrenTransitions>
                <RepositionThemeTransition />
            </StackPanel.ChildrenTransitions>
            <controls:SettingsCard x:Name="TestBestConfigSettingsCard" l:Uids.Uid="TestBestConfigSettingsCard" IsClickEnabled="True" Click="TestBestConfigSettingsCard_Click">
                <controls:SettingsCard.HeaderIcon>
                    <BitmapIcon UriSource="ms-appx:///Assets/Icons/GoodCheck.png"  ShowAsMonochrome="False" Width="20" Height="20"/>
                </controls:SettingsCard.HeaderIcon>
                <controls:SettingsCard.ActionIcon>
                    <FontIcon Glyph="&#xE8A7;" />
                </controls:SettingsCard.ActionIcon>
            </controls:SettingsCard>
            <controls:SettingsCard x:Name="BeginNewSelectionButton" l:Uids.Uid="CreateNewViaGoodCheckSettingsCard" IsClickEnabled="True" Click="BeginNewSelectionButton_Click">
                <controls:SettingsCard.HeaderIcon>
                    <BitmapIcon UriSource="ms-appx:///Assets/Icons/GoodCheck.png"  ShowAsMonochrome="False" Width="20" Height="20"/>
                </controls:SettingsCard.HeaderIcon>
            </controls:SettingsCard>
            <controls:SettingsCard l:Uids.Uid="CreateYouOwnConfigSettingsCard" IsClickEnabled="True" Click="ViewOtherButton_Click">
                <controls:SettingsCard.Header>
                    <TextBlock l:Uids.Uid="OpenConfigHelperTextBlock" TextWrapping="Wrap" TextAlignment="Left" VerticalAlignment="Bottom" HorizontalAlignment="Stretch"/>
                </controls:SettingsCard.Header>
                <controls:SettingsCard.HeaderIcon>
                    <BitmapIcon UriSource="ms-appx:///Assets/Icons/Edit.png"  ShowAsMonochrome="False" Width="20" Height="20"/>
                </controls:SettingsCard.HeaderIcon>
                <controls:SettingsCard.ActionIcon>
                    <FontIcon Glyph="&#xE8A7;" />
                </controls:SettingsCard.ActionIcon>
            </controls:SettingsCard>
        </StackPanel>

        <StackPanel x:Name="PlaceholderGrid" Grid.Row="1" Margin="20 20 20 0" Spacing="20">
            <TextBlock l:Uids.Uid="NoOneComponentInstalledTextBlock" TextWrapping="Wrap"/>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <Button x:Name="GetNewComponentsFromStoreButton" Height="35" Click="GetNewComponentsFromStoreButton_Click">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <BitmapIcon UriSource="ms-appx:///Assets/Icons/Store.png" Width="16" ShowAsMonochrome="True"/>
                        <TextBlock l:Uids.Uid="GetNewComponentsFromStoreTextBlock" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
        <Grid x:Name="ActionButtonsGrid" Grid.Row="3">
            <Rectangle Fill="{ThemeResource ControlFillColorSecondaryBrush}"/>

            <Grid Padding="20">
                <Button x:Name="GetHelpButton" l:Uids.Uid="GetHelpButton" Padding="6" Click="GetHelpButton_Click">
                    <Button.Content>
                        <FontIcon Glyph="&#xE897;" FontSize="16"/>
                    </Button.Content>
                </Button>
                <StackPanel Orientation="Horizontal" Spacing="10"  HorizontalAlignment="Right">
                    <Button x:Name="CancelButton" l:Uids.Uid="ExitButton" MinWidth="150" Click="CancelButton_Click" IsEnabled="True"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
