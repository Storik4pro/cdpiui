<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GoodbyeDPI_UI.Views.CreateConfigUtil.GoodCheckWorkPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoodbyeDPI_UI.Views.CreateConfigUtil"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid x:Name="ContentGrid" Padding="20, 0, 20, 0" Visibility="Visible">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel x:Name="LoadingStackPanel" Grid.Row="1">
                <StackPanel.RenderTransform>
                    <TranslateTransform/>
                </StackPanel.RenderTransform>
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Подбор настроек запуска"/>
                <RichTextBlock Margin="0 15" TextWrapping="Wrap" HorizontalAlignment="Left" IsTextSelectionEnabled="False">
                    <Paragraph>
                        <Run Text="Этот процесс займет некоторое время. Отличный шанс потрогать траву"/>
                    </Paragraph>
                </RichTextBlock>
            </StackPanel>
            <Grid Grid.Row="2" CornerRadius="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0 0 0 5" Spacing="5">
                    <FontIcon Glyph="&#xE9D9;" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="Подробности" Style="{ThemeResource BodyStrongTextBlockStyle}"  VerticalAlignment="Center"/>
                </StackPanel>
                <HyperlinkButton x:Name="ViewMoreButton" HorizontalAlignment="Right" Padding="0" Margin="0 0 0 5" Click="ViewMoreButton_Click" 
                                 PointerEntered="ViewMoreButton_PointerEntered" PointerExited="ViewMoreButton_PointerExited">
                    <HyperlinkButton.Content>
                        <TextBlock x:Name="ViewMoreText" Text="Показать больше" TextDecorations="Underline"/>
                    </HyperlinkButton.Content>
                </HyperlinkButton>

                <Rectangle Grid.Row="1" Fill="{ThemeResource ControlFillColorSecondaryBrush}"/>
                <StackPanel Grid.Row="1" Padding="10" HorizontalAlignment="Stretch">
                    <TextBlock Text="Общее" Style="{ThemeResource BodyStrongTextBlockStyle}" />

                    <TextBlock Text="Сейчас идет подбор для: "/>
                    <TextBlock x:Name="SiteListProgressText" Style="{ThemeResource CaptionTextBlockStyle}" Margin="50 0 0 0"/>
                    <TextBlock Text="Оценка оставшегося времени: "/>
                    <TextBlock x:Name="AllTimeText" Text="Расчет..." Style="{ThemeResource CaptionTextBlockStyle}" Margin="50 0 0 0"/>
                    
                    <StackPanel x:Name="AdditionalInfo" Visibility="Collapsed">
                        <Rectangle Fill="{ThemeResource SmokeFillColorDefaultBrush}" Height="2" Margin="0 5 0 5"/>
                        <TextBlock Text="Текущая проверка" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                        <TextBlock Text="Оценка оставшегося времени: "/>
                        <TextBlock x:Name="TimeText" Text="Расчет..." Style="{ThemeResource CaptionTextBlockStyle}" Margin="50 0 0 0"/>
                        <TextBlock Text="Процент выполненной работы: "/>
                        <TextBlock x:Name="SiteListProgress" Style="{ThemeResource CaptionTextBlockStyle}" Margin="50 0 0 0"/>
                        <TextBlock Text="Количество неудачных стратегий (Менее 65%): "/>
                        <TextBlock x:Name="BadStrategyCount" Style="{ThemeResource CaptionTextBlockStyle}" Margin="50 0 0 0"/>
                        <TextBlock Text="Количество удачных стратегий (Более 65%): "/>
                        <TextBlock x:Name="GoodStrategyCount" Style="{ThemeResource CaptionTextBlockStyle}" Margin="50 0 0 0"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
            
        </Grid>

        <Grid x:Name="EndWork" Padding="20, 0, 20, 0" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="1">
                <StackPanel.RenderTransform>
                    <TranslateTransform/>
                </StackPanel.RenderTransform>
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Подбор завершён"/>
                <RichTextBlock Margin="0 15" TextWrapping="Wrap" HorizontalAlignment="Left" IsTextSelectionEnabled="False">
                    <Paragraph>
                        <Run Text="Нажмите на кнопку Далее, чтобы перейти к просмотру результата."/>
                    </Paragraph>
                </RichTextBlock>
            </StackPanel>

        </Grid>

        <Grid x:Name="LoadingStateGrid" Grid.Row="1" Visibility="Visible" Margin="0 20 0 0">
            <StackPanel>
                <TextBlock x:Name="LoadingHeader" Text="Идет работа над этим..." Margin="20 0 0 5" Visibility="Collapsed"/>
                <ProgressBar x:Name="LoadingProgressBar" IsIndeterminate="True" VerticalAlignment="Top" CornerRadius="0"/>
            </StackPanel>
        </Grid>
        <Grid x:Name="ActionButtonsGrid" Grid.Row="2">
            <Rectangle Fill="{ThemeResource ControlFillColorSecondaryBrush}"/>

            <Grid Padding="20">
                <Button x:Name="GetHelpButton" ToolTipService.ToolTip="Получить справку" Padding="6" Click="GetHelpButton_Click">
                    <Button.Content>
                        <FontIcon Glyph="&#xE897;" FontSize="16"/>
                    </Button.Content>
                </Button>
                <StackPanel Orientation="Horizontal" Spacing="10"  HorizontalAlignment="Right">
                    <RichTextBlock Margin="0" TextWrapping="Wrap" IsTextSelectionEnabled="False" VerticalAlignment="Center">
                        <Paragraph>
                            <Hyperlink x:Name="ViewLogHyperlink" Click="ViewLogHyperlink_Click">
                                Открыть логи процесса
                            </Hyperlink>
                        </Paragraph>
                    </RichTextBlock>
                    <Button x:Name="CancelButton" Content="Остановить" MinWidth="150">
                        <Button.Flyout>
                            <Flyout>
                                <StackPanel Spacing="5">
                                    <TextBlock Text="Остановка" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Text="Выберите вариант остановки."/>
                                    <Button x:Name="KillProc" Content="Завершить текущий процесс" HorizontalAlignment="Stretch" Click="KillProc_Click"/>
                                    <Button x:Name="EndAll" Content="Завершить подбор полностью" HorizontalAlignment="Stretch" Click="EndAll_Click"/>
                                </StackPanel>
                            </Flyout>
                        </Button.Flyout>
                    </Button>
                    <Button x:Name="ForwardButton" Content="Далее" Style="{ThemeResource AccentButtonStyle}" MinWidth="150" Visibility="Collapsed" Click="ForwardButton_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
