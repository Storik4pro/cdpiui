<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDPI_UI.Views.Components.ViewComponentSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:l="using:WinUI3Localizer"
    xmlns:lcontrols="using:CDPI_UI">

    
    
    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid Margin="20,10,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock
                x:Name="PageHeader"
                Style="{StaticResource TitleTextBlockStyle}"
                TextWrapping="NoWrap"/>
            <CheckBox x:Name="AutorunCheckBox" l:Uids.Uid="AutorunCheckBox" Grid.Column="1" VerticalAlignment="Center" Click="AutorunCheckBox_Click"/>
        </Grid>
        <Frame x:Name="PageContentFrame" Grid.Row="1" />
    </Grid>
</Page>
