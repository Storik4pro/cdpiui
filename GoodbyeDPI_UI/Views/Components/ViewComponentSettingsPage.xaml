<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDPI_UI.Views.Components.ViewComponentSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:l="using:WinUI3Localizer"
    xmlns:lcontrols="using:CDPI_UI">

    <Page.Resources>
        <x:Double x:Key="SettingsCardSpacing">4</x:Double>
    </Page.Resources>
    
    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource Breakpoint640Plus}" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="contentSV.Padding" Value="16,0" />
                        <Setter Target="PageHeader.Margin" Value="16,20,0,0" />
                        <Setter Target="ConfigChooseCombobox.MinWidth" Value="300" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1382"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainPanel.HorizontalAlignment" Value="Center"/>
                        <Setter Target="MainPanel.Width" Value="1060px"/>
                        <Setter Target="EmptyPageGrid.HorizontalAlignment" Value="Center"/>
                        <Setter Target="EmptyPageGrid.Width" Value="1060px"/>
                        <Setter Target="PageHeader.HorizontalAlignment" Value="Center"/>
                        <Setter Target="PageHeader.Width" Value="1060px"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>

                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <TextBlock
            x:Name="PageHeader"
            Margin="20,24,20,10"
            Style="{StaticResource TitleTextBlockStyle}"
            TextWrapping="NoWrap"/>
        <ScrollViewer
            x:Name="contentSV"
            Grid.Row="1"
            Padding="20,0,20,0"
            IsTabStop="False"
            UseSystemFocusVisuals="False"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <Grid>
                <StackPanel x:Name="MainPanel" Spacing="{StaticResource SettingsCardSpacing}" Margin="0 0 0 10" Visibility="Collapsed">
                    <controls:SettingsCard l:Uids.Uid="ConfigSettingsCard"
                                   CornerRadius="10"
                                   BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}">
                        <ComboBox x:Name="ConfigChooseCombobox" 
                                  MinWidth="400"
                                  SelectionChanged="ConfigChooseCombobox_SelectionChanged"
                                  Height="47">
                            <ComboBox.ItemTemplate>
                                <DataTemplate x:DataType="local:ComboboxItem">
                                    <StackPanel>
                                        <TextBlock Text="{Binding name}"/>
                                        <TextBlock Text="{Binding packName}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiary}"/>
                                    </StackPanel>
                                </DataTemplate>

                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </controls:SettingsCard>

                    <ItemsRepeater x:Name="StaggeredRepeater" ElementPrepared="StaggeredRepeater_ElementPrepared">
                        <ItemsRepeater.Layout>
                            <controls:StaggeredLayout
                        ColumnSpacing="5"
                        RowSpacing="5"
                        DesiredColumnWidth="360"
                        ItemsStretch="Fill"/>
                        </ItemsRepeater.Layout>
                    </ItemsRepeater>
                </StackPanel>
                <Grid x:Name="EmptyPageGrid">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Text="(◡︵◡)" Style="{ThemeResource SubheaderTextBlockStyle}"/>
                        <TextBlock l:Uids.Uid="EmptyPageTitle" Style="{ThemeResource SubheaderTextBlockStyle}" TextWrapping="NoWrap"/>
                        <TextBlock l:Uids.Uid="EmptyPageDescription" TextWrapping="Wrap"/>
                    </StackPanel>
                    <VariableSizedWrapGrid Orientation="Horizontal" Grid.Row="1" Margin="20" ItemHeight="50">
                        <Button x:Name="OpenStoreButton" Padding="6" Margin="0 0 10 0" Height="40" Click="OpenStoreButton_Click">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <BitmapIcon UriSource="ms-appx:///Assets/Icons/Store.png" Width="23" ShowAsMonochrome="True"/>
                                <TextBlock l:Uids.Uid="ExploreInStore" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="CreateConfigButton" Grid.Column="1" Padding="6" Margin="0 0 10 0" Height="40" Click="CreateConfigButton_Click">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <BitmapIcon UriSource="ms-appx:///Assets/Store/empty.png" Width="23" ShowAsMonochrome="True"/>
                                <TextBlock l:Uids.Uid="CreateConfigTextBlock" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </VariableSizedWrapGrid>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
