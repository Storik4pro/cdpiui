<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDPI_UI.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls">

    <Page.Resources>
        <x:Double x:Key="SettingsCardSpacing">4</x:Double>
    </Page.Resources>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource Breakpoint640Plus}" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="contentSV.Padding" Value="16,0" />
                        <Setter Target="PageHeader.Margin" Value="16,20,0,0" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1382"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainPanel.HorizontalAlignment" Value="Center"/>
                        <Setter Target="MainPanel.Width" Value="1060px"/>
                        <Setter Target="PageHeader.HorizontalAlignment" Value="Center"/>
                        <Setter Target="PageHeader.Width" Value="1060px"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>

                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <TextBlock
            l:Uids.Uid="AboutPageTextBlock"
            x:Name="PageHeader"
            Margin="20,24,20,10"
            Style="{StaticResource TitleTextBlockStyle}"
            TextWrapping="NoWrap"/>
        <ScrollViewer
            x:Name="contentSV"
            Grid.Row="1"
            Padding="20,0,20,0"
            IsTabStop="False"
            UseSystemFocusVisuals="False"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <StackPanel x:Name="MainPanel" Margin="0 0 0 20">
                <controls:SettingsCard>
                    <controls:SettingsCard.Resources>
                        <x:Double x:Key="SettingsCardWrapThreshold">700</x:Double>
                        <x:Double x:Key="SettingsCardWrapNoIconThreshold">600</x:Double>
                    </controls:SettingsCard.Resources>

                    <controls:SettingsCard.HeaderIcon>
                        <ImageIcon Source="/Assets/StoreLogo.png"/>
                    </controls:SettingsCard.HeaderIcon>
                    <controls:SettingsCard.Header>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <TextBlock Text="CDPI UI" Style="{ThemeResource SubheaderTextBlockStyle}" FontSize="16"/>
                            <Grid x:Name="StatusBadgeGrid">
                                <Border x:Name="UpdateBadgeBorder" Background="#4CA0E0" CornerRadius="5"/>
                                <StackPanel Orientation="Horizontal" Spacing="5" Padding="5 2">
                                    <FontIcon x:Name="UpdateBadgeGlyph" Glyph="&#xE930;" FontSize="16" Foreground="Black"/>
                                    <TextBlock x:Name="UpdateBadgeTextBlock" FontWeight="SemiLight"  Foreground="Black"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <StackPanel Margin="0 5 0 0">
                            <TextBlock x:Name="CurrentVersionTextBlock" IsTextSelectionEnabled="True"/>
                            <TextBlock x:Name="ServerVersionTextBlock" Visibility="Collapsed" IsTextSelectionEnabled="True"/>
                        </StackPanel>
                    </controls:SettingsCard.Description>
                    <Button x:Name="UpdateButton" Style="{ThemeResource AccentButtonStyle}" Click="UpdateButton_Click">
                        <Grid>
                            <TextBlock x:Name="UpdateButtonTextBlock" Text="Check for updates"/>
                            <ProgressRing x:Name="UpdateProgressRing" IsIndeterminate="True" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" Width="18" Height="18" Visibility="Collapsed" />
                        </Grid>
                    </Button>
                </controls:SettingsCard>
                
                
                <TextBlock l:Uids.Uid="UsefulLinksTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}" Margin="0 10 0 0"/>
                <StackPanel>
                    <HyperlinkButton x:Name="DonateButton" Click="DonateButton_Click" Padding="0 5 5 5">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <FontIcon Glyph="&#xEB51;" FontSize="16" />
                            <TextBlock l:Uids.Uid="DonateTextBlock"/>
                        </StackPanel>
                    </HyperlinkButton>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <HyperlinkButton x:Name="DeveloperChannelButton" Click="DeveloperChannelButton_Click" Padding="0 5 5 5">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <FontIcon Glyph="&#xF003;" FontSize="16" />
                                <TextBlock l:Uids.Uid="DeveloperChannelTextBlock"/>
                            </StackPanel>
                        </HyperlinkButton>
                        <FontIcon Glyph="&#xE7EA;" FontSize="14" />
                        <TextBlock l:Uids.Uid="DeveloperChannelTipTextBlock" VerticalAlignment="Center"/>
                    </StackPanel>
                    <HyperlinkButton x:Name="ReportABugButton" Click="ReportABugButton_Click" Padding="0 5 5 5">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <FontIcon Glyph="&#xEBE8;" FontSize="16" />
                            <TextBlock l:Uids.Uid="ReportABugTextBlock"/>
                        </StackPanel>
                    </HyperlinkButton>
                </StackPanel>
                
                
                <TextBlock l:Uids.Uid="AboutAppTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}" Margin="0 10 0 5"/>
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="© 2025 Storik4. License: Apache License, Version 2.0." IsTextSelectionEnabled="True" TextWrapping="Wrap" VerticalAlignment="Center"/>
                        <HyperlinkButton x:Name="LicenseButton" Grid.Column="1" Content="More..." Padding="0" Click="LicenseButton_Click"/>
                    </Grid>
                    <TextBlock l:Uids.Uid="DeveloperTextBlock" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondary}"/>
                    <TextBlock Margin="20 0 0 0" Text="Storik4" IsTextSelectionEnabled="True"/>
                    <TextBlock l:Uids.Uid="SourceTextBlock" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondary}"/>
                    <RichTextBlock Margin="20 0 0 0">
                        <Paragraph>
                            <Hyperlink x:Name="RepoHyperlink" Click="RepoHyperlink_Click">
                                <Run x:Name="RepoRun"/>
                            </Hyperlink>
                        </Paragraph>
                    </RichTextBlock>
                    <TextBlock l:Uids.Uid="AcknowledgmentsTextBlock" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondary}"/>
                    <ListView Margin="20 0 0 0" x:Name="AcknowledgmentsListView" ItemContainerStyle="{StaticResource ListViewSmallItemStretchStyle}">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:AcknowledgmentsModel">
                                <StackPanel Orientation="Horizontal" Spacing="10" Height="20">
                                    <FontIcon Glyph="&#xE77B;" FontSize="14"/>
                                    <TextBlock Text="{x:Bind Name, Mode=OneWay}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <TextBlock l:Uids.Uid="RequirementsTextBlock" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondary}"/>
                    <ListView Margin="20 0 0 0" x:Name="RequirementsListView" ItemContainerStyle="{StaticResource ListViewSmallItemStretchStyle}">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:AcknowledgmentsModel">
                                <StackPanel Orientation="Horizontal" Spacing="10" Height="20">
                                    <TextBlock Text="{x:Bind Name, Mode=OneWay}" VerticalAlignment="Center" IsTextSelectionEnabled="True"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
