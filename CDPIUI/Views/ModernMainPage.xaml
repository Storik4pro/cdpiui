<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Name="MainPage"
    x:Class="CDPIUI.Views.ModernMainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPIUI.Views"
    xmlns:lcontrols="using:CDPIUI"
    xmlns:mpcontrols="using:CDPIUI.Controls.MainPage"
    xmlns:ucontrols="using:CDPIUI.Controls.Universal"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer">

    <Grid x:Name="RootGrid" Padding="20 0" SizeChanged="RootGrid_SizeChanged">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="SmallState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ContentGrid.HorizontalAlignment" Value="Stretch" />
                        <Setter Target="ContentGrid.Width" Value="auto" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1100"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ContentGrid.HorizontalAlignment" Value="Center" />
                        <Setter Target="ContentGrid.Width" Value="1060" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        
        <Grid x:Name="ContentGrid" Padding="0 20">
            <Grid.RowDefinitions>
                <RowDefinition Height="250"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid>
                <mpcontrols:WelcomePlaceholderUserControl x:Name="ComponentTilePlaceholder"/>
                <ucontrols:HorizontalScrollContainer x:Name="ComponentTilesScrollContainer" Visibility="Collapsed" Margin="-5 0">
                    <ucontrols:HorizontalScrollContainer.Source>
                        <GridView x:Name="MainGridView" SelectionMode="None">
                            <GridView.ItemTemplate>
                                <DataTemplate x:DataType="local:ComponentTileModel">
                                    <mpcontrols:ComponentTileUserControl 
                                        HorizontalAlignment="Stretch"
                                        Height="{x:Bind Height, Mode=OneWay}" 
                                        StoreId="{x:Bind Id, Mode=OneWay}" 
                                        ImageUrl="{x:Bind ImageSource, Mode=OneWay}" />
                                </DataTemplate>
                            </GridView.ItemTemplate>
                            <GridView.ItemContainerStyle>
                                <Style TargetType="GridViewItem" BasedOn="{StaticResource DefaultGridViewItemStyle}">
                                    <Setter Property="Margin" Value="5, 0, 5, 0"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                </Style>
                            </GridView.ItemContainerStyle>

                            <GridView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VariableSizedWrapGrid x:Name="PanelTemplateStackPanel" MaximumRowsOrColumns="1" ItemWidth="{Binding ElementWidth}"/>
                                </ItemsPanelTemplate>
                            </GridView.ItemsPanel>
                        </GridView>
                    </ucontrols:HorizontalScrollContainer.Source>
                </ucontrols:HorizontalScrollContainer>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock l:Uids.Uid="QuickAccessTextBlock" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0 5 0 5" Visibility="Collapsed"/>
                <Grid x:Name="WidgetPlaceholderGrid" Grid.Row="1" Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center" Spacing="10">
                        <FontIcon Glyph="&#xECA5;"  FontSize="28" HorizontalAlignment="Center"/>
                        <TextBlock l:Uids.Uid="WidgetPlaceholderTextBlock" HorizontalAlignment="Center" Style="{ThemeResource BaseTextBlockStyle}"/>
                    </StackPanel>
                </Grid>

                <Grid x:Name="WidgetGrid" Grid.Row="1" Margin="0 10 0 0">
                    <mpcontrols:WidgetUserControl/>
                </Grid>

                <ItemsRepeater x:Name="WidgetsItemsRepeater" Grid.Row="1">
                    <ItemsRepeater.Layout>
                        <toolkit:StaggeredLayout ItemsStretch="Fill"
                                             ColumnSpacing="10"
                                             DesiredColumnWidth="400"/>
                    </ItemsRepeater.Layout>
                </ItemsRepeater>
            </Grid>
            <HyperlinkButton x:Name="QHyperlinkButton" l:Uids.Uid="QHyperlinkButton" Grid.Row="2" Margin="0 0 0 -20" 
                         Content="..." VerticalAlignment="Bottom" HorizontalAlignment="Right" Padding="10 0" CornerRadius="0" 
                         Click="QHyperlinkButton_Click"/>
        </Grid>
    </Grid>
</Page>
