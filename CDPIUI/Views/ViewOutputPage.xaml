<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDPI_UI.Views.ViewOutputPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Page.Resources>
        <Style x:Key="ListViewItemStretchStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="MaxHeight" Value="10" />
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <ListViewItemPresenter ContentMargin="0" Padding="0" Margin="0"  IsTabStop="False" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="FuncBar" Height="32"/>
            <RowDefinition x:Name="ViewHeader" Height="Auto"/>
            <RowDefinition x:Name="View" Height="*"/>
            <RowDefinition x:Name="StatusBar" Height="28"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Height="36">
            <Rectangle Fill="{ThemeResource LayerOnMicaBaseAltFillColorDefault}" Height="32" Width="Auto" VerticalAlignment="Top"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="Menu" Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition x:Name="Quick" Width="Auto" />
                </Grid.ColumnDefinitions>
                <MenuBar Grid.Column="0" Margin="0" Height="32" MinHeight="32" VerticalAlignment="Top" Padding="0">
                    <MenuBarItem Title="File" Margin="5, 0">
                        <MenuFlyoutItem x:Name="SaveButton" Text="Save output to file" Click="SaveButton_Click" >
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE74E;" />
                            </MenuFlyoutItem.Icon>
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Modifiers="Control" Key="S"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem x:Name="ExitButton" Text="Exit" Click="ExitButton_Click" >
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE711;" />
                            </MenuFlyoutItem.Icon>
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Modifiers="Menu" Key="F4"/>
                            </MenuFlyoutItem.KeyboardAccelerators>

                        </MenuFlyoutItem>
                    </MenuBarItem>

                    <MenuBarItem Title="Process" Margin="5, 0">
                        <MenuFlyoutItem x:Name="ProcessControl" Text="Kill Current Process" Click="ProcessControl_Click">
                            <MenuFlyoutItem.Icon>
                                <FontIcon x:Name="ProcessControlIcon" Glyph="&#xE71A;" />
                            </MenuFlyoutItem.Icon>
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Modifiers="Control" Key="F5"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem x:Name="ProcessExit" Text="End all operations" Click="ProcessExit_Click" >
                        </MenuFlyoutItem>
                    </MenuBarItem>

                    <MenuBarItem Title="View" Margin="5, 0">
                        <MenuFlyoutItem x:Name="ChangeFont" Text="Font" Click="ChangeFont_Click" >
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE8D2;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutSubItem Text="AutoScroll">
                            <RadioMenuFlyoutItem x:Name="EnableAutoScrollButton" Text="Enable" GroupName="OrientationGroup" Click="EnableAutoScrollButton_Click"/>
                            <RadioMenuFlyoutItem x:Name="DisableAutoScrollButton" Text="Disable" GroupName="OrientationGroup" Click="DisableAutoScrollButton_Click"/>
                            <MenuFlyoutSubItem.Icon>
                                <FontIcon Glyph="&#xECE7;" />
                            </MenuFlyoutSubItem.Icon>
                        </MenuFlyoutSubItem>
                    </MenuBarItem>

                    <MenuBarItem Title="Help" Margin="5, 0">
                        <MenuFlyoutItem x:Name="SupportButton" Text="Visit GitHub page" Click="SupportButton_Click">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE8A7;" />
                            </MenuFlyoutItem.Icon>
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Key="F1"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>

                    </MenuBarItem>
                </MenuBar>
            </Grid>
        </Grid>
        <ScrollViewer x:Name="TargetScrollViewer"
                      Grid.Row="2"
                      Background="Black"
                      Padding="20 0 20 0">
            <ListView x:Name="LogListView"
                      SelectionMode="None"
                      IsItemClickEnabled="False"
                      IsSwipeEnabled="False"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Grid.Row="2"
                      ItemContainerStyle="{StaticResource ListViewItemStretchStyle}"
                      IsTabStop="False">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsStackPanel ItemsUpdatingScrollMode="KeepLastItemInView"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemContainerTransitions>
                    <TransitionCollection>

                    </TransitionCollection>
                </ListView.ItemContainerTransitions>

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <ListViewItem
                            Height="18"
                            MinHeight="18">
                            <TextBlock Text="{Binding}"
                                   Foreground="White"
                                   FontFamily="Consolas"
                                   FontSize="16"
                                   TextWrapping="NoWrap"
                                   Margin="0"/>
                        </ListViewItem>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
        </ScrollViewer>
        <ScrollViewer x:Name="OutputScrollViewer" Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Background="Black" Visibility="Collapsed">

            <RichTextBlock x:Name="OutputRichTextBlock"
                            FontFamily="Consolas"
                            FontSize="16"
                            Foreground="White"
                            TextWrapping="Wrap"
                            IsTextSelectionEnabled="True"
                            Padding="20"
                            SelectionHighlightColor="Gray">
                <Paragraph x:Name="OutputParagraph"/>
            </RichTextBlock>

        </ScrollViewer>
        <Grid Grid.Row="3">
            <Rectangle Fill="{ThemeResource LayerOnMicaBaseAltFillColorDefault}" Height="28" Width="Auto" VerticalAlignment="Top"/>
            <Grid Padding="10, 0, 10, 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <FontIcon x:Name="StatusIcon" Grid.Column="4" Glyph="&#xEC61;" FontSize="16" Visibility="Collapsed" />
                <ProgressRing x:Name="WorkIndicator" Grid.Column="4" IsIndeterminate="True" Width="10" Height="10"/>
                <TextBlock x:Name="StatusText" Grid.Column="5" Text="Process worked" VerticalAlignment="Center"/>
                
                <Rectangle Grid.Column="3" Fill="Gray" Height="15" Width="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
