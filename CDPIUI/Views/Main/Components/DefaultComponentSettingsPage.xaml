<?xml version="1.0" encoding="utf-8"?>
<tpl:TemplatePage
    x:Class="CDPIUI.Views.Main.Components.DefaultComponentSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPIUI.Views.Main.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:l="using:WinUI3Localizer"
    xmlns:lcontrols="using:CDPIUI"
    xmlns:componentControls="using:CDPIUI.Controls.ComponentSettings"
    xmlns:tpl="using:CDPIUI.Controls.Default">

    <tpl:TemplatePage.Resources>
        <x:Double x:Key="SettingsCardSpacing">4</x:Double>
    </tpl:TemplatePage.Resources>

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource Breakpoint640Plus}" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="contentSV.Padding" Value="16,0" />
                        <Setter Target="PageHeader.Margin" Value="16,10,0,0" />
                        <Setter Target="ConfigChooseCombobox.MinWidth" Value="300" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1382"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainPanel.HorizontalAlignment" Value="Center"/>
                        <Setter Target="MainPanel.Width" Value="1060px"/>
                        <Setter Target="EmptyPageGrid.HorizontalAlignment" Value="Center"/>
                        <Setter Target="EmptyPageGrid.Width" Value="1060px"/>
                        <Setter Target="PageHeader.HorizontalAlignment" Value="Center"/>
                        <Setter Target="PageHeader.Width" Value="1060px"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>

                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <ScrollViewer
            x:Name="contentSV"
            Grid.Row="1"
            Padding="20,0,20,0"
            IsTabStop="False"
            UseSystemFocusVisuals="False"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <Grid Margin="0 10 0 10">
                <StackPanel x:Name="MainPanel" Spacing="{StaticResource SettingsCardSpacing}" Margin="0 0 0 10" Visibility="Collapsed">
                    <controls:SettingsCard l:Uids.Uid="ConfigSettingsCard"
                                           CornerRadius="10"
                                           BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}">
                        <controls:SettingsCard.Resources>
                            <x:Double x:Key="SettingsCardWrapNoIconThreshold">700</x:Double>
                        </controls:SettingsCard.Resources>
                        <componentControls:ConfigSelector
                            x:Name="ConfigChooseCombobox"
                            MinWidth="400"
                            Height="47"
                            SelectionChanged="ConfigChooseCombobox_SelectionChanged" />
                    </controls:SettingsCard>

                    <ItemsRepeater x:Name="StaggeredRepeater" ElementPrepared="StaggeredRepeater_ElementPrepared">
                        <ItemsRepeater.Layout>
                            <controls:StaggeredLayout
                                ColumnSpacing="5"
                                RowSpacing="5"
                                DesiredColumnWidth="360"
                                ItemsStretch="Fill"/>
                        </ItemsRepeater.Layout>
                    </ItemsRepeater>
                </StackPanel>
                <Grid x:Name="LoadingGrid" Margin="0 200 0 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <ProgressRing Grid.Row="0" HorizontalAlignment="Center"/>
                    <TextBlock  Grid.Row="1" l:Uids.Uid="WorkingOnItTextBlock" HorizontalAlignment="Center"/>
                </Grid>
                <Grid x:Name="EmptyPageGrid" Visibility="Collapsed">
                    <componentControls:ConfigNeededUserControl x:Name="EmptyPlaceholder"/>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</tpl:TemplatePage>
