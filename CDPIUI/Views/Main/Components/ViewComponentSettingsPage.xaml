<?xml version="1.0" encoding="utf-8"?>
<tpl:TemplatePage
    x:Name="RootPage"
    x:Class="CDPIUI.Views.Main.Components.ViewComponentSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPIUI.Views.Main.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:l="using:WinUI3Localizer"
    xmlns:lcontrols="using:CDPIUI"
    xmlns:mpcontrols="using:CDPIUI.Controls.MainPage"
    xmlns:tpl="using:CDPIUI.Controls.Default">

    

    <Grid x:Name="RootGrid" Background="Transparent" AllowDrop="True" DragOver="RootGrid_DragOver" DragLeave="RootGrid_DragLeave" Drop="RootGrid_Drop">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="SmallState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootGrid.HorizontalAlignment" Value="Stretch" />
                        <Setter Target="RootGrid.Width" Value="auto" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1100"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootGrid.HorizontalAlignment" Value="Center" />
                        <Setter Target="RootGrid.Width" Value="1100" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid.ChildrenTransitions>
            <RepositionThemeTransition/>
        </Grid.ChildrenTransitions>
        
        

        <Grid Margin="0">

            <mpcontrols:SmallComponentTileUserControl 
                x:Name="ComponentTileUserControl"
                IsOpened="True"
                ShowSettingsCommand="{Binding ShowComponentSettingsClickCommand, ElementName=RootPage}"/>

        </Grid>
        <Frame x:Name="PageContentFrame" Grid.Row="2" />

        <Grid Grid.RowSpan="3" x:Name="DragGrid" Visibility="Collapsed">
            <Border Background="{ThemeResource AccentAcrylicBackgroundFillColorBaseBrush}" Opacity="0.8"/>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="10">
                <FontIcon Glyph="&#xE8B0;" HorizontalAlignment="Center" FontSize="28"/>
                <TextBlock l:Uids.Uid="DropFilesTextBlock" Style="{ThemeResource SubtitleTextBlockStyle}" TextAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</tpl:TemplatePage>
