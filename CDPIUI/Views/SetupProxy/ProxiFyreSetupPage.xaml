<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Name="MainPage"
    x:Class="CDPI_UI.Views.SetupProxy.ProxiFyreSetupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Views.SetupProxy"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls" 
    xmlns:lcontrol="using:CDPI_UI"
    xmlns:conv="using:CDPI_UI.Converters">

    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <ScrollViewer Padding="20 0">
                <StackPanel Spacing="20">
                    <TextBlock l:Uids.Uid="SetupViaProxiFyreTextBlock" Style="{StaticResource TitleTextBlockStyle}"/>

                    <StackPanel Spacing="10">
                        <controls:SettingsCard l:Uids.Uid="ProxiFyreModeCard">
                            <ComboBox x:Name="ProxiFyreModeComboBox" DisplayMemberPath="DisplayName"/>
                        </controls:SettingsCard>

                        <controls:SettingsCard x:Name="ApplicationWhiteListCard" l:Uids.Uid="ApplicationWhiteListCard" IsClickEnabled="True" Click="ApplicationWhiteListCard_Click"/>
                        <controls:SettingsCard x:Name="ApplicationBlackListCard" l:Uids.Uid="ApplicationBlackListCard"  IsClickEnabled="True" Click="ApplicationBlackListCard_Click"/>

                        <ToggleButton x:Name="ProxySettingsVisibleToggleButton">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock l:Uids.Uid="ProxyAddrSettingsTextBlock"/>
                                <FontIcon Glyph="&#xE70D;" FontSize="16"/>
                            </StackPanel>
                        </ToggleButton>
                        <Grid Width="400" HorizontalAlignment="Left" Visibility="{Binding IsChecked, ElementName=ProxySettingsVisibleToggleButton, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock l:Uids.Uid="ProxyIpAddressTextBlock"/>
                                <TextBox x:Name="IPValue" HorizontalAlignment="Stretch"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock l:Uids.Uid="PortTextBlock"/>
                                <TextBox x:Name="PortValue" HorizontalAlignment="Stretch"/>
                            </StackPanel>
                            <Button x:Name="ResetButton" l:Uids.Uid="ResetByDefaultButton"
                                    Grid.Column="3" Padding="6" 
                                    VerticalAlignment="Bottom" Margin="10 0 0 0" 
                                    Height="32" Width="32"
                                    Click="ResetButton_Click">
                                <FontIcon Glyph="&#xE777;" FontSize="16"/>
                            </Button>
                        </Grid>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Grid x:Name="ActionButtonsGrid" Grid.Row="2">
            <Rectangle Fill="{ThemeResource ControlFillColorSecondaryBrush}"/>

            <Grid Padding="20">
                <Button x:Name="GetHelpButton" l:Uids.Uid="GetHelpButton" Padding="6" Click="GetHelpButton_Click">
                    <Button.Content>
                        <FontIcon Glyph="&#xE897;" FontSize="16"/>
                    </Button.Content>
                </Button>
                <StackPanel Orientation="Horizontal" Spacing="10"  HorizontalAlignment="Right">
                    <Button l:Uids.Uid="GoBackButton" x:Name="GoBackButton" MinWidth="150" Click="GoBackButton_Click"/>
                    <Button l:Uids.Uid="GoForwardButton" x:Name="GoForwardButton" Style="{StaticResource AccentButtonStyle}" MinWidth="150" Click="GoForwardButton_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
