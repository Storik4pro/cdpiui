<?xml version="1.0" encoding="utf-8"?>
<tpl:TemplatePage
    x:Class="CDPIUI.Views.Store.Settings.Memory.MemoryViewConditionalLaunchDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:CDPIUI.ViewModels"
    xmlns:l="using:WinUI3Localizer"
    xmlns:tpl="using:CDPIUI.Controls.Default"
    mc:Ignorable="d">

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="SmallState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainStackPanel.HorizontalAlignment" Value="Stretch" />
                        <Setter Target="MainStackPanel.Width" Value="Auto" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1975" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainStackPanel.HorizontalAlignment" Value="Center" />
                        <Setter Target="MainStackPanel.Width" Value="1860" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <ScrollViewer Padding="20,0">
            <StackPanel x:Name="MainStackPanel" Margin="0,20" Spacing="8">
                <Grid x:Name="NavGrid" Margin="0,0,0,10">
                    <Rectangle Fill="Transparent" />
                    <BreadcrumbBar x:Name="BreadcrumbBar" ItemClicked="BreadcrumbBar_ItemClicked">
                        <BreadcrumbBar.ItemTemplate>
                            <DataTemplate x:DataType="viewmodels:BreadcrumbBarModel">
                                <BreadcrumbBarItem Content="{Binding}" AutomationProperties.Name="{Binding DisplayName}" Tag="{Binding Tag}">
                                    <BreadcrumbBarItem.ContentTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding DisplayName}" Style="{ThemeResource TitleTextBlockStyle}" />
                                        </DataTemplate>
                                    </BreadcrumbBarItem.ContentTemplate>
                                </BreadcrumbBarItem>
                            </DataTemplate>
                        </BreadcrumbBar.ItemTemplate>
                    </BreadcrumbBar>
                </Grid>

                <TextBlock x:Name="MemoryTextBlock" Style="{ThemeResource SubheaderTextBlockStyle}" />
                <TextBlock x:Name="TaskCountTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                <TextBlock l:Uids.Uid="ConditionalTasksFullDescriptionTextBlock" MaxWidth="560" HorizontalAlignment="Left" TextWrapping="Wrap" />

                <TextBlock l:Uids.Uid="ActionsTextBlock" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,10,0,0" />
                <Button l:Uids.Uid="ManageConditionalTasksButton" Click="ManageConditionalTasksButton_Click" />

                <TextBlock l:Uids.Uid="CleanupTextBlock" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,10,0,0" />
                <TextBlock l:Uids.Uid="CleanupConditionalTasksTextBlock" MaxWidth="560" HorizontalAlignment="Left" TextWrapping="Wrap" />
                <Button x:Name="DeleteAllTasksButton" l:Uids.Uid="DeleteAllConditionalTasksButton" Click="DeleteAllTasksButton_Click" />

                <InfoBar x:Name="StatusInfoBar" Margin="0,8,0,0" IsOpen="False" IsClosable="True" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</tpl:TemplatePage>
