<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDPI_UI.Views.Store.Settings.Memory.MemoryViewSettingsDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Views.Store.Settings.Memory"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer"
    xmlns:viewmodels="using:CDPI_UI.ViewModels">

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="SmallState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainStackPanel.HorizontalAlignment" Value="Stretch" />
                        <Setter Target="MainStackPanel.Width" Value="auto" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1975"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainStackPanel.HorizontalAlignment" Value="Center" />
                        <Setter Target="MainStackPanel.Width" Value="1860" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <ScrollViewer Padding="20 0">
            <StackPanel x:Name="MainStackPanel" Spacing="5" Margin="0 20">
                <Grid Margin="0 0 0 10" x:Name="NavGrid">
                    <Rectangle Fill="Transparent"/>
                    <BreadcrumbBar x:Name="BreadcrumbBar" ItemClicked="BreadcrumbBar_ItemClicked">
                        <BreadcrumbBar.ItemTemplate>
                            <DataTemplate x:DataType="viewmodels:BreadcrumbBarModel">
                                <BreadcrumbBarItem Content="{Binding}" AutomationProperties.Name="{Binding DisplayName}" Tag="{Binding Tag}">
                                    <BreadcrumbBarItem.ContentTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding DisplayName}" Style="{ThemeResource TitleTextBlockStyle}"/>
                                        </DataTemplate>
                                    </BreadcrumbBarItem.ContentTemplate>
                                </BreadcrumbBarItem>
                            </DataTemplate>
                        </BreadcrumbBar.ItemTemplate>
                    </BreadcrumbBar>
                </Grid>
                <TextBlock x:Name="MemoryTextBlock" Style="{ThemeResource SubheaderTextBlockStyle}"/>
                <TextBlock l:Uids.Uid="ApplicationSettingsFullDescriptionTextBlock" MaxWidth="450" HorizontalAlignment="Left" TextWrapping="Wrap"/>

                <TextBlock l:Uids.Uid="ActionsTextBlock" Style="{ThemeResource SubtitleTextBlockStyle}"  Margin="0 10 0 0"/>
                <TextBlock l:Uids.Uid="ResetSettingsTextBlock" TextWrapping="Wrap"/>
                <Button x:Name="ResetButton" l:Uids.Uid="ResetButton">
                    <Button.Flyout>
                        <Flyout x:Name="RemoveFlyout" Placement="BottomEdgeAlignedLeft">
                            <Grid Width="350">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock l:Uids.Uid="AreYouSureTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                <TextBlock Grid.Row="1" l:Uids.Uid="ResetSettingsAskTextBlock" TextWrapping="Wrap"/>
                                <Grid Grid.Row="2" Margin="0 10 0 0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="5"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button l:Uids.Uid="ContinueAnywayButton" Click="ResetButton_Click" HorizontalAlignment="Stretch"/>
                                    <Button Grid.Column="2" l:Uids.Uid="CancelButton" x:Name="CancelButton" Click="CancelButton_Click" Style="{ThemeResource AccentButtonStyle}" HorizontalAlignment="Stretch"/>
                                </Grid>
                            </Grid>
                        </Flyout>
                    </Button.Flyout>
                </Button>
                <Grid x:Name="ErrorStackPanel" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Glyph="&#xEA39;" FontSize="16"/>
                    <TextBlock x:Name="ErrorTextBlock" Grid.Column="2" TextWrapping="Wrap" IsTextSelectionEnabled="True"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
