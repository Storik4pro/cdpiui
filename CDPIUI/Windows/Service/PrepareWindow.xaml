<?xml version="1.0" encoding="utf-8"?>
<template:TemplateWindow
    x:Name="MainWindow"
    x:Class="CDPI_UI.PrepareWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Getting ready application (CDPI UI)..."
    xmlns:template="using:CDPI_UI.Default"
    xmlns:wc="using:CDPI_UI.Controls.WindowControls">

    <template:TemplateWindow.SystemBackdrop>
        <MicaBackdrop />
    </template:TemplateWindow.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <wc:TitleBarUserControl 
            x:Name="TitleBarUserControl"
            Height="48"/>
        <Grid x:Name="ContentGrid" VerticalAlignment="Bottom" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <FontIcon Glyph="&#xF133;" FontSize="32" HorizontalAlignment="Left" Margin="20 0 0 0" />
            <StackPanel Padding="20" Grid.Row="1">
                <TextBlock Text="We're preparing this app (CDPI UI) for you" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
            </StackPanel>
            <ProgressBar IsIndeterminate="True" Grid.Row="2"/>
        </Grid>
    </Grid>
</template:TemplateWindow>
