<?xml version="1.0" encoding="utf-8"?>
<Button
    x:Class="CDPIUI.Controls.Universal.LargeButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPIUI.Controls.Universal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:CDPIUI.Converters"
    mc:Ignorable="d"
    Style="{ThemeResource DefaultButtonStyle}"
    Width="120" 
    Height="120" 
    Padding="6" 
    VerticalContentAlignment="Stretch" 
    HorizontalContentAlignment="Stretch">

    <Button.Resources>
        <Style TargetType="FontIcon">
            <Setter Property="FontSize" Value="24"/>
        </Style>
        <conv:StringToVisibilityConverter x:Key="StringToVis"/>
    </Button.Resources>

    <Grid>
        <FontIcon Glyph="{x:Bind ActionIcon, Mode=OneWay}" FontSize="12" VerticalAlignment="Top" HorizontalAlignment="Right" Visibility="{x:Bind ActionIcon, Mode=OneWay, Converter={StaticResource StringToVis}}" Margin="5"/>
        <IconSourceElement IconSource="{x:Bind Icon, Mode=OneWay}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="5"/>
        <TextBlock Text="{x:Bind Text, Mode=OneWay}" TextWrapping="Wrap" TextAlignment="Left" VerticalAlignment="Bottom" HorizontalAlignment="Stretch"/>
    </Grid>
</Button>
