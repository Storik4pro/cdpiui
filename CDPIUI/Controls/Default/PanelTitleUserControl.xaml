<UserControl
    x:Class="CDPIUI.Controls.Default.PanelTitleUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:l="using:WinUI3Localizer"
    MinHeight="36"
    xmlns:conv="using:CDPIUI.Converters">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Name="BoolToVis"/>
    </UserControl.Resources>

    <Border Background="{ThemeResource SubtleFillColorSecondaryBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Margin="10,0"
                VerticalAlignment="Center"
                Text="{x:Bind Text, Mode=OneWay}"
                Style="{ThemeResource CaptionTextBlockStyle}" />

            <ContentPresenter
                Grid.Column="1"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                Content="{x:Bind AdditionalContent, Mode=OneWay}"
                Margin="0 0 5 0"/>

            <Button
                Grid.Column="2"
                Margin="0,0,10,0"
                Padding="6"
                CornerRadius="0"
                Height="29"
                l:Uids.Uid="HideButton"
                Click="CloseButton_Click"
                Visibility="{x:Bind CloseEnabled, Mode=OneWay, Converter={StaticResource BoolToVis}}">

                <FontIcon Glyph="&#xE70D;" FontSize="14" />
            </Button>
        </Grid>
    </Border>
</UserControl>
