<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="CDPI_UI.Controls.Dialogs.ComponentSettings.CreateNewConfigForComponentContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Controls.Dialogs.ComponentSettings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer"
    l:Uids.Uid="CreateNewConfigForComponentContentDialog"
    Style="{ThemeResource DefaultContentDialogStyle}">

    <Grid MinWidth="350">
        <StackPanel Orientation="Horizontal" Spacing="10">
            <Button x:Name="CreateFromTemplateButton" Width="120" Height="120" Padding="6" Click="CreateFromTemplateButton_Click"  VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Button.Content>
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="25"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <FontIcon Glyph="&#xE8A5;" HorizontalAlignment="Left" />
                        <TextBlock l:Uids.Uid="CreateFromTemplateTextBlock" Grid.Row="1" TextWrapping="Wrap" TextAlignment="Left" VerticalAlignment="Bottom" HorizontalAlignment="Left"/>
                    </Grid>
                </Button.Content>
            </Button>
            <Button x:Name="CreateManuallyButton" Width="120" Height="120" Padding="6" Click="CreateManuallyButton_Click"  VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Button.Content>
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="25"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <ImageIcon x:Name="CreateManuallyIcon" HorizontalAlignment="Left" Source="/Assets/Icons/Edit.png"/>
                        <TextBlock l:Uids.Uid="CreateManuallyTextBlock" Grid.Row="1" TextWrapping="Wrap" TextAlignment="Left" VerticalAlignment="Bottom" HorizontalAlignment="Left"/>
                    </Grid>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</ContentDialog>
