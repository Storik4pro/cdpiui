<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    l:Uids.Uid="CommunityContentDialog"
    x:Class="CDPIUI.Controls.Dialogs.MainPage.CommunityContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPIUI.Controls.Dialogs.MainPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer"
    xmlns:viewmodels="using:CDPIUI.ViewModels"
    PrimaryButtonText="OK"
    Style="{ThemeResource DefaultContentDialogStyle}">

    <Grid>
        <ListView x:Name="MainListView" ItemContainerStyle="{StaticResource ListViewItemStretchStyle}" Margin="0 0 -15 0">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:UrlViewModel">
                    <Button Padding="6" Click="Button_Click" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{x:Bind Name, Mode=OneWay}" Style="{ThemeResource BodyStrongTextBlockStyle}" MaxLines="1"/>
                                <TextBlock Text="{x:Bind Description, Mode=OneWay}" TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind Url, Mode=OneWay}" IsTextSelectionEnabled="True" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" LineHeight="1" FontStyle="Italic" HorizontalAlignment="Left"/>
                            </StackPanel>
                            <FontIcon Glyph="&#xE8A7;" FontSize="12" Grid.Column="2"/>
                        </Grid>
                    </Button>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Spacing="5" Margin="0 0 15 0"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </Grid>
</ContentDialog>
