<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="CDPI_UI.Controls.Dialogs.Universal.MarkdownTextViewContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Controls.Dialogs.Universal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:controls="using:CommunityToolkit.Labs.WinUI.MarkdownTextBlock"
    mc:Ignorable="d"
    PrimaryButtonText="OK">

    <Grid>
        <ScrollViewer>
            <controls:MarkdownTextBlock 
                Margin="20 5 0 5" x:Name="TextBlock" 
                Config="{x:Bind MarkdownConfig, Mode=OneTime}"
                Text="{x:Bind Text, Mode=OneWay}"
                UseAutoLinks="True"
                UseEmphasisExtras="True"
                UseListExtras="True"
                UsePipeTables="True"
                UseTaskLists="True"/>
        </ScrollViewer>
    </Grid>
</ContentDialog>
