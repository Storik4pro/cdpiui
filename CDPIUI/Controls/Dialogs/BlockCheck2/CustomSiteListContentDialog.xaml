<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="CDPIUI.Controls.Dialogs.BlockCheck2.CustomSiteListContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:l="using:WinUI3Localizer"
    Style="{ThemeResource DefaultContentDialogStyle}"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">720</x:Double>
    </ContentDialog.Resources>

    <Grid Width="520" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock
            l:Uids.Uid="BlockCheck2CustomSiteListDialogDescriptionText"
            TextWrapping="Wrap" />
        <InfoBar
            x:Name="ValidationInfoBar"
            IsClosable="False"
            IsOpen="False"
            Severity="Warning" Grid.Row="1" />
        <ScrollView Grid.Row="2" Margin="0 0 -20 0">
            <TextBox
                Margin="0 0 20 0"
                x:Name="SiteListTextBox"
                l:Uids.Uid="BlockCheck2CustomSiteListTextBox"
                MinHeight="230"
                AcceptsReturn="True"
                IsSpellCheckEnabled="False"
                TextWrapping="Wrap"
                ScrollViewer.VerticalScrollBarVisibility="Auto" />
        </ScrollView>
    </Grid>
</ContentDialog>
