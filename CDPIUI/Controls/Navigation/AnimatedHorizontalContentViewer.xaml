<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPIUI.Controls.Navigation.AnimatedHorizontalContentViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPIUI.Controls.Navigation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:conv="using:CDPIUI.Converters">

    <UserControl.Resources>
        <Storyboard x:Name="UpdateTextStoryboard">
            <DoubleAnimation AutoReverse="True" Storyboard.TargetName="TitleTextBlock" Storyboard.TargetProperty="Opacity" From="1" To="0.75" Duration="0:0:0.05" />
            <DoubleAnimation AutoReverse="True" Storyboard.TargetName="DescriptionTextBlock" Storyboard.TargetProperty="Opacity" From="1" To="0.75" Duration="0:0:0.05" />
        </Storyboard>
        <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock 
            x:Name="TitleTextBlock" 
            Text="{x:Bind CurrentHeader, Mode=OneWay}"
            Style="{ThemeResource TitleTextBlockStyle}" 
            Grid.Row="0" 
            TextWrapping="NoWrap" 
            Opacity="1" 
            Margin="20 0 0 5"
            Visibility="{x:Bind CurrentHeader, Mode=OneWay, Converter={StaticResource StringToVisibilityConverter}}"/>
        
        <TextBlock 
            x:Name="DescriptionTextBlock"
            Text="{x:Bind CurrentDescription, Mode=OneWay}"
            Grid.Row="1" 
            TextWrapping="Wrap" 
            Margin="20 0 20 5" 
            Visibility="{x:Bind CurrentDescription, Mode=OneWay, Converter={StaticResource StringToVisibilityConverter}}"/>

        <Grid x:Name="ContentHost" Grid.Row="2">
            <ContentPresenter
                x:Name="PrimaryPresenter"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch">
                <ContentPresenter.RenderTransform>
                    <TranslateTransform />
                </ContentPresenter.RenderTransform>
            </ContentPresenter>
            <ContentPresenter
                x:Name="SecondaryPresenter"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                Visibility="Collapsed">
                <ContentPresenter.RenderTransform>
                    <TranslateTransform />
                </ContentPresenter.RenderTransform>
            </ContentPresenter>
        </Grid>
    </Grid>
</UserControl>
