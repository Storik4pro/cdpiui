<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPI_UI.Controls.Store.StoreAskForSubItemInstallUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Controls.Store"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Button Padding="0" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" HorizontalAlignment="Stretch" Click="Button_Click"
            ToolTipService.ToolTip="{x:Bind Description, Mode=OneWay}">
        <Grid>
            <Grid Height="50" Margin="6 6 6 6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0"
                        BorderThickness="1" CornerRadius="5" 
                        BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}"/>
                <Image x:Name="PART_Image" Grid.Column="0" Source="{x:Bind CardImageSource, Mode=OneWay}" Width="30"/>
                <StackPanel Grid.Column="1" Padding="10 0 5 0" VerticalAlignment="Center">
                    <TextBlock Text="{x:Bind CardTitle, Mode=OneWay}" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <TextBlock Text="{x:Bind Developer, Mode=OneWay}"/>
                        <Rectangle VerticalAlignment="Stretch" Width="1" Fill="{ThemeResource ControlFillColorSecondaryBrush}"/>
                        <HyperlinkButton x:Name="ItemCategoryButton" Content="{x:Bind CardCategory, Mode=OneWay}" Padding="0, 0"/>
                    </StackPanel>
                </StackPanel>
                <FontIcon x:Name="StatusFontIcon" Grid.Column="2" Glyph="&#xEBD3;" FontSize="16" VerticalAlignment="Bottom"/>
            </Grid>
            <ProgressBar x:Name="StatusProgressBar" VerticalAlignment="Bottom" Background="Transparent" IsIndeterminate="True" Visibility="Collapsed"/>
        </Grid>
    </Button>
</UserControl>
