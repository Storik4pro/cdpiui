<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Name="RootControl"
    x:Class="CDPIUI.Controls.Store.StoreViewBundles"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPIUI.Controls.Store"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    MinHeight="230"
    Visibility="Collapsed">

    <Grid x:Name="MainGrid">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="LayoutStates">
                <VisualState x:Name="CompactLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootControl.BundleLayoutMode" Value="Compact"/>
                        <Setter Target="MainGrid.Padding" Value="0 0 15 0"/>
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="MediumLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="700"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootControl.BundleLayoutMode" Value="Medium"/>
                        <Setter Target="MainGrid.Padding" Value="0 0 15 0"/>
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="WideLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1200"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootControl.BundleLayoutMode" Value="Wide"/>
                        <Setter Target="MainGrid.Padding" Value="0 0 15 0"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <ItemsRepeater ItemsSource="{x:Bind Bundles, Mode=OneWay}">
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="local:StoreViewBundleItem">
                    <local:StoreReadyKitButton
                        MinWidth="0"
                        KitId="{x:Bind KitId, Mode=OneWay}"
                        CardImageSource="{x:Bind CardImageSource, Mode=OneWay}"
                        CardTitle="{x:Bind CardTitle, Mode=OneWay}"
                        CardSubtitle="{x:Bind CardSubtitle, Mode=OneWay}"
                        CardBackgroundBrush="{x:Bind CardBackgroundBrush, Mode=OneWay}"
                        Items="{x:Bind Items, Mode=OneWay}"
                        IconSize="70"
                        Click="BundleButton_Click"/>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
            <ItemsRepeater.Layout>
                <local:StoreBundlesLayout
                    LayoutMode="{x:Bind BundleLayoutMode, Mode=OneWay}"
                    ItemHeight="110"  
                    MinimumItemWidth="260"
                    ColumnSpacing="10"
                    RowSpacing="10"
                    LargeColumnWeight="7"
                    SmallColumnWeight="5"
                    BasicColumnWeight="1"/>
            </ItemsRepeater.Layout>
        </ItemsRepeater>
    </Grid>
</UserControl>
