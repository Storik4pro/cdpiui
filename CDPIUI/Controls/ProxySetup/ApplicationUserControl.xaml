<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPI_UI.ApplicationUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer">

    <Grid>
        <Border Background="{ThemeResource ControlFillColorDefaultBrush}" CornerRadius="5" BorderBrush="{ThemeResource ControlStrokeColorOnAccentSecondaryBrush}" BorderThickness="1"/>
        <Grid Padding="10 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal" Spacing="10">
                <FontIcon Glyph="&#xE74C;" FontSize="16" VerticalAlignment="Center"/>
                <StackPanel>
                    <TextBlock Text="{x:Bind DisplayName, Mode=OneWay}"/>
                    <TextBlock Text="{x:Bind FullPath, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" IsTextSelectionEnabled="True"/>
                </StackPanel>
            </StackPanel>
            <Button l:Uids.Uid="RemoveButton" Padding="6" Grid.Column="1" Click="Button_Click">
                <FontIcon Glyph="&#xE711;" FontSize="16"/>
            </Button>
        </Grid>
    </Grid>
</UserControl>
