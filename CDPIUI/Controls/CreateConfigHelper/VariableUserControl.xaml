<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPI_UI.VariableUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer">

    <Grid x:Name="RootGrid" Margin="0 2.5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Border Grid.ColumnSpan="3" Grid.RowSpan="3"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="5" />

        <StackPanel x:Name="VariableNameStackPanel"
                    Grid.Row="0"
                    Margin="10 10 10 0"
                    ToolTipService.ToolTip="{x:Bind VarName, Mode=OneWay}">
            <TextBlock Text="{x:Bind VarName, Mode=OneWay}" IsTextSelectionEnabled="True" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
        </StackPanel>

        <StackPanel x:Name="VariableValueStackPanel"
                    Grid.Row="1"
                    Margin="10 0"
                    ToolTipService.ToolTip="{x:Bind VarValue, Mode=OneWay}">
            <TextBlock l:Uids.Uid="ValueTextBox" Style="{ThemeResource CaptionTextBlockStyle}"/>
            <TextBlock Text="{x:Bind VarValue, Mode=OneWay}"
                   IsTextSelectionEnabled="True"/>
        </StackPanel>

        <StackPanel x:Name="ActionsPanel"
                    Grid.Row="2"
                    Margin="10 10"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="10">
            <Button x:Name="EditButton" Padding="6" Click="EditButton_Click">
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <FontIcon Glyph="&#xE70F;" FontSize="16" />
                    <TextBlock l:Uids.Uid="EditValueTextBox"/>
                </StackPanel>
            </Button>
            <Button Padding="6" x:Name="CopyButton" l:Uids.Uid="CopyButton" Click="CopyButton_Click" Height="33" Width="33">
                <FontIcon Glyph="&#xE8C8;" FontSize="16" />
            </Button>
            <Button Padding="6" l:Uids.Uid="RemoveButton" Height="33" Width="33">
                <FontIcon Glyph="&#xE711;" FontSize="16" />
                <Button.Flyout>
                    <Flyout x:Name="RemoveFlyout">
                        <StackPanel Width="300">
                            <TextBlock l:Uids.Uid="AreYouSureTextBlock" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                            <TextBlock l:Uids.Uid="ActionCannotBeUndoTextBlock"/>
                            <StackPanel Orientation="Horizontal" Spacing="10" Margin="0 10 0 0">
                                <Button x:Name="RemoveButton" l:Uids.Uid="YesRemoveButton" HorizontalAlignment="Stretch" Click="RemoveButton_Click"/>
                                <Button x:Name="CloseFlyout" 
                                        Style="{ThemeResource AccentButtonStyle}" 
                                        HorizontalAlignment="Stretch" Click="CloseFlyout_Click">
                                    <TextBlock l:Uids.Uid="NoCancelTextBlock"/>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
