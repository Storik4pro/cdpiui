<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPIUI.Controls.MainPage.SmallComponentTileUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPIUI.Controls.MainPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:media="using:Microsoft.UI.Xaml.Media"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:triggers="using:CommunityToolkit.WinUI"
    xmlns:lcontrols="using:CDPIUI"
    xmlns:viewmodels="using:CDPIUI.ViewModels"
    xmlns:l="using:WinUI3Localizer">

    

    <Grid x:Name="RootGrid">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="SSmallView">
                    <VisualState.StateTriggers>
                        <triggers:ControlSizeTrigger TargetElement="{Binding ElementName=RootGrid}" CanTrigger="True" MaxWidth="600"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ImageGrid.Visibility" Value="Collapsed"/>
                        <Setter Target="ImageColumn.Width" Value="0"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup>
                <VisualState x:Name="NormalView">
                    <VisualState.StateTriggers>
                        <triggers:ControlSizeTrigger TargetElement="{Binding ElementName=RootGrid}"
                                                     CanTrigger="{x:Bind IsOpened, Mode=OneWay}"
                                                     MinWidth="1000"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ComponentSettingsCard.CornerRadius" Value="5"/>
                        <Setter Target="ImageGrid.CornerRadius" Value="0"/>
                        <Setter Target="ComponentSettingsCard.Margin" Value="20 20 20 0"/>
                        <Setter Target="ShowMoreSettingsGlyph.Glyph" Value="&#xE70E;"/>

                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="StretchView">
                    <VisualState.StateTriggers>
                        <triggers:ControlSizeTrigger TargetElement="{Binding ElementName=RootGrid}"
                                                     CanTrigger="{x:Bind IsOpened, Mode=OneWay}"
                                                     MaxWidth="1000"
                                                     MinWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ComponentSettingsCard.CornerRadius" Value="0"/>
                        <Setter Target="ImageGrid.CornerRadius" Value="0"/>
                        <Setter Target="ComponentSettingsCard.Margin" Value="0"/>
                        <Setter Target="ShowMoreSettingsGlyph.Glyph" Value="&#xE70E;"/>

                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>


        </VisualStateManager.VisualStateGroups>

        <Grid.ChildrenTransitions>
            <RepositionThemeTransition/>
        </Grid.ChildrenTransitions>

        <controls:SettingsCard x:Name="ComponentSettingsCard" Padding="0" HorizontalContentAlignment="Center" Height="68" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}">
            <controls:SettingsCard.Resources>
                <x:Double x:Key="SettingsCardWrapThreshold">20</x:Double>
                <x:Double x:Key="SettingsCardWrapNoIconThreshold">20</x:Double>
            </controls:SettingsCard.Resources>

            <controls:SettingsCard.Header>
                <Grid HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="ImageColumn" Width="65"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid x:Name="ImageGrid" MinHeight="65" Height="{x:Bind ComponentSettingsCard.ActualHeight, Mode=OneWay}" Width="65" CornerRadius="5">
                        <Rectangle x:Name="ImageBackgroundGradientRectangle" 
                                   VerticalAlignment="Stretch" HorizontalAlignment="Stretch" RadiusX="10" RadiusY="10">
                            <Rectangle.Fill>
                                <SolidColorBrush x:Name="MainBrush"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <Rectangle>
                            <Rectangle.Fill>
                                <media:AcrylicBrush x:Name="AcrylicBrush" TintOpacity="0.7"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <Grid>
                            <Image x:Name="PART_Image" Source="{x:Bind CardImageSource, Mode=OneWay}" Width="30" Height="30"/>
                            <Grid x:Name="StatusGrid" Height="20" Width="20">
                                <Grid.RenderTransform>
                                    <TranslateTransform X="10" Y="10"/>
                                </Grid.RenderTransform>
                                <FontIcon x:Name="BackgroundStatusFontIcon" Glyph="&#xE91F;" Foreground="{ThemeResource SystemFillColorSuccessBrush}" FontSize="18"/>
                                <FontIcon x:Name="ForegroundStatusFontIcon" Glyph="&#xF13E;" Foreground="Black" FontSize="10" Margin="1 0 0 0" FontWeight="Bold"/>
                                <FontIcon x:Name="ForegroundStopStatusFontIcon" Glyph="&#xE7E8;" Foreground="Black" FontSize="14" Margin="0 0 0 0" FontWeight="Bold"/>
                                <FontIcon Glyph="&#xEA3A;" Foreground="Black" FontSize="20"/>
                            </Grid>
                        </Grid>
                    </Grid>
                    <Grid Grid.Column="2" VerticalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{x:Bind CardTitle, Mode=OneWay}" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                        <StackPanel Orientation="Horizontal" Grid.Row="1">
                            <CheckBox x:Name="AutorunCheckBox" l:Uids.Uid="AutorunCheckBox" Click="AutorunCheckBox_Click"/>
                        </StackPanel>
                        
                    </Grid>
                </Grid>
            </controls:SettingsCard.Header>
            <Grid Grid.Column="4" Margin="0 0 20 0">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <ToggleButton x:Name="PlayButton" Padding="6" Click="PlayButton_Click">
                        <ToolTipService.ToolTip>
                            <TextBlock x:Name="PlayToolTip"/>
                        </ToolTipService.ToolTip>
                        <FontIcon x:Name="PlayGlyph" Glyph="&#xE768;" FontSize="16"/>
                    </ToggleButton>
                    <Button x:Name="ViewOutputButton" l:Uids.Uid="ViewOutputButton" Padding="6" Click="ViewOutputButton_Click">
                        <FontIcon Glyph="&#xE721;" FontSize="16"/>
                    </Button>
                    <Button x:Name="ShowMoreSettingsButton" Padding="6" Click="ShowMoreSettingsButton_Click">
                        <FontIcon x:Name="ShowMoreSettingsGlyph" Glyph="&#xE70D;" FontSize="16"/>
                        <ToolTipService.ToolTip>
                            <TextBlock x:Name="ShowMoreSettingsToolTip"/>
                        </ToolTipService.ToolTip>
                    </Button>
                </StackPanel>
            </Grid>
        </controls:SettingsCard>
    </Grid>
</UserControl>
