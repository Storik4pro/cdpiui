<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPIUI.Controls.ComponentSettings.ConfigNeededUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPIUI.Controls.ComponentSettings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer"
    xmlns:control="using:CommunityToolkit.WinUI.Controls"
    xmlns:vm="using:CDPIUI.ViewModels">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock l:Uids.Uid="YouMustGetConfigsFirstTextBlock" FontStyle="Italic"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0 10 0 10" Spacing="10">
            <TextBlock l:Uids.Uid="YouCanGetConfigsFromThatSourceTextBlock" TextWrapping="Wrap"/>

            <ListView x:Name="AvailableActionsListView" ItemContainerStyle="{StaticResource ListViewItemStretchStyle}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:AvailableConfigCreationActionModel">
                        <control:SettingsCard
                            Header="{x:Bind Name, Mode=OneWay}"
                            Description="{x:Bind Description, Mode=OneWay}"
                            Tag="{x:Bind Action, Mode=OneWay}"
                            IsClickEnabled="True"
                            Click="SettingsCard_Click">
                            <control:SettingsCard.HeaderIcon>
                                <BitmapIcon UriSource="{x:Bind IconUri, Mode=OneWay}" ShowAsMonochrome="False" Width="20" Height="20"/>
                            </control:SettingsCard.HeaderIcon>

                            <control:SettingsCard.ActionIcon>
                                <FontIcon Glyph="{x:Bind ActionIconGlyph, Mode=OneWay}"/>
                            </control:SettingsCard.ActionIcon>
                        </control:SettingsCard>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Spacing="5"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>
        </StackPanel>

        <Grid Grid.Row="4" Margin="0 0 0 10">
            <Border 
                 Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                 BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                 BorderThickness="1" 
                 CornerRadius="5"/>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid Padding="10 5" ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <FontIcon Grid.Column="0" Glyph="&#xE754;" />
                    <TextBlock Grid.Column="1" l:Uids.Uid="/Flashlight/ExplainTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                </Grid>

                <Border 
                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" 
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                    BorderThickness="1" 
                    CornerRadius="5"
                    Grid.Row="1"/>

                <TextBlock Grid.Row="1" l:Uids.Uid="/Flashlight/YouMustGetConfigsFirstTextBlock" TextWrapping="Wrap" Margin="10"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
