<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPI_UI.SettingTileControlElement"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls" 
    xmlns:winuicontrols="using:System.Windows.Controls">

    <UserControl.Resources>
        <local:GlyphToFontIconConverter x:Key="GlyphToFontIconConverter"/>
    </UserControl.Resources>

    <Grid MinHeight="55">
        <Rectangle 
            x:Name="TopRectangle"
            Height="1" 
            HorizontalAlignment="Stretch" 
            Fill="{ThemeResource CardStrokeColorDefaultSolidBrush}" 
            VerticalAlignment="Top"/>
        <controls:SettingsCard x:Name="SettingsCard"
                               Header="{x:Bind Title}" 
                               HeaderIcon="{x:Bind HeaderIconGlyph, Converter={StaticResource GlyphToFontIconConverter}}"
                               CornerRadius="0" 
                               Background="Transparent" 
                               BorderBrush="Transparent"
                               Padding="{x:Bind CardPadding}"
                               IsClickEnabled="{x:Bind IsClickEnabled}"
                               Click="SettingsCard_Click"
                               MinHeight="55">
            
            <controls:SettingsCard.ActionIcon>
                <FontIcon Glyph="{x:Bind ActionIconGlyph}"/>
            </controls:SettingsCard.ActionIcon>
            
            <controls:SettingsCard.Resources>
                <x:Double x:Key="SettingsCardWrapThreshold">100</x:Double>
                <x:Double x:Key="SettingsCardWrapNoIconThreshold">200</x:Double>
            </controls:SettingsCard.Resources>
            
            <ContentPresenter Content="{x:Bind InnerContent, Mode=OneWay}" VerticalAlignment="Center" />
        </controls:SettingsCard>
    </Grid>
</UserControl>
