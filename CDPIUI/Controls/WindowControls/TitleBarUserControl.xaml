<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CDPI_UI.Controls.WindowControls.TitleBarUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDPI_UI.Controls.WindowControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:l="using:WinUI3Localizer">

    <Grid x:Name="AppTitleBar"
          Canvas.ZIndex="1"
          Height="{x:Bind Height, Mode=OneWay}"
          IsHitTestVisible="True"
          VerticalAlignment="Top"
          Margin="0, 0, 0, 0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1" Margin="0, 0, 0, 0" Padding="0, 0, 0, 0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="48"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>
            <Grid.ChildrenTransitions>
                <RepositionThemeTransition/>
            </Grid.ChildrenTransitions>
            <ContentPresenter x:Name="ControlsContentPresenter" MinWidth="{x:Bind ControlsContentMinWidth, Mode=OneWay}" Grid.Column="0" Content="{x:Bind ControlsContent, Mode=OneWay}" Visibility="Collapsed"/>
            <Grid x:Name="ImageAera" Grid.Column="1" Height="{x:Bind Height, Mode=OneWay}" x:FieldModifier="public">
                <Image 
                    Grid.Column="0"
                    Source="{x:Bind IconSource, Mode=OneWay}"
                    Width="16" Height="16"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"/>
                <Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Fill="Transparent"/>
            </Grid>
            <Grid x:Name="WindowMoveAera" Grid.Column="3" Grid.ColumnSpan="5" x:FieldModifier="public"/>
            <TextBlock 
                Text="{x:Bind Title, Mode=OneWay}"
                x:Name="AppTitleTextBlock"
                TextWrapping="NoWrap"
                Style="{StaticResource CaptionTextBlockStyle}"
                VerticalAlignment="Center"
                Grid.Column="3" 
                Margin="0, 0, 0, 0"
                x:FieldModifier="public"/>
            <TextBlock 
                l:Uids.Uid="PreviewTextBlock"
                x:Name="AppTitleSubTextBlock" 
                Grid.Column="4"
                TextWrapping="NoWrap"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                VerticalAlignment="Center"
                Margin="8, 0, 0, 0"
                Visibility="{x:Bind PreviewHintVisibility, Mode=OneWay}"/>
            <ContentPresenter Grid.Column="5" Content="{x:Bind AdditionalContent, Mode=OneWay}"/>
        </Grid>
    </Grid>
</UserControl>
